Wait — I don't understand where you've got to here.

Re-pitch your last answer. Give me a little context first, use ASD-STE100 Simplified
Technical English, and use the ubiquitous language from `CONTEXT.md`.

Three rules for the re-pitch:

- **Do not add material.** This is the same answer said differently, not a longer one.
  If the original was wrong, say so in one line and correct it — but do not quietly
  fold new content into a re-pitch.
- **Lead with the signal-processing reading when one exists.** The strongest questions
  in this repo's record were SP unifications the reader generated unprompted (matched
  filter, adaptive filter, log-likelihood ratio, whitening). If the mechanism has a
  clean SP analogue, that is the pitch.
- **Name the step that did not land, and start one step earlier than that.** A re-pitch
  that restarts from the top wastes the part that already worked.

Adapted from `mattpocock-skills:wait-what`. This is a reader-invoked escape hatch inside
a `/study` session (`.claude/commands/study.md`): an explanation that does not land is a
signal about the pitch, not about the reader.
