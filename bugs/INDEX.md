# Bugs Index

| date | id | title | severity | status | hook |
|---|---|---|---|---|---|
| 2026-06-30 | 2026-06-30-02 | §A.11 said a head has "six raw matrices"; it has four (W_Q, W_K, W_V, W_O) | low | fixed | miscount in prose + Figure A.11 caption + figure title; a head has 4 projections (QK: W_Q,W_K; OV: W_V,W_O), §A.4 gauge d_k²+d_v²; fixed text + regenerated svg during the §A.11 explicit-params enrichment |
| 2026-06-30 | 2026-06-30-01 | Figure A.10 caption reuses d_k for two scales (demo d_k=8 vs grounded d_k=64) within one caption, reading as a contradiction | low | fixed | both values correct; symbol collision + undisclosed demo dims; reader-flagged; caption-only disambiguation (demo scale stated numerically, each d_k tagged demo/grounded) |
| 2026-06-29 | 2026-06-29-01 | §A.19 worked example used the row/data-matrix convention, clashing with §A.1's column convention | med | fixed | numbers right but X∈R^{T×d}/W_Q∈R^{d×d_k} clashed with §A.1's column form and made "W_OV=W_O W_V" dimensionally false; rewrote §A.19 + Fig A.12 to column convention (same values, transposed) |
| 2026-06-28 | 2026-06-28-01 | Relative asset links (link to figures/*.svg) 404 in viewer SPA — href not rewritten to doc dir | med | fixed | viewer rewrote img src but not a[href]; figures/*.svg link resolved to root /figures/ → 404; fixRelativePaths now rewrites asset hrefs too |
| 2026-06-13 | 2026-06-13-01 | Magicoder base HumanEval figure misattributed (WizardCoder-CL's 48.2 used for Code Llama-Python-7B) | med | fixed | citation-audit caught wrong base value in LLMs-for-code §8; fixed 48.2→37.8 |
| 2026-06-17 | 2026-06-17-01 | serve.js /api/md/<empty-or-dir> crashes the whole server process (unhandled EISDIR) | med | fixed | FIXED 2026-07-05: `/api/md/` guard now requires isFile() → clean 404; applied to both copies (byte-convergent), regression test proven red-without-fix |
| 2026-06-19 | 2026-06-19-01 | Highlighting a span that starts inside inline math and crosses a second math span highlights the whole paragraph | med | fixed | viewer routed multi-span start-in-katex selections to the whole-block sidecar; now routes to PLAIN_SPANNING_MATH for a precise inline highlight |
| 2026-06-20 | 2026-06-20-01 | Appendix E.5 over-attributed Llama-2-70B's 64-head count to the Llama-2 paper [63] | low | fixed | citation-audit caught H=64 not in [63]'s text (derived/config value); reworded to cite only the stated 8-KV-group GQA design; non-load-bearing |
| 2026-07-01 | 2026-07-01-01 | Two drifted numeric values in the mechanistic-interpretability survey (ROME AIE attribution; RepE LoRRA deltas) | med | fixed | citation-audit caught 8.7%→MLP misattribution (should be 6.6% MLP / 8.7% individual-state) and LoRRA +6.6/+13.1→+11.3/+11.6; both from secondary sources, corrected vs primary |
| 2026-07-02 | 2026-07-02-01 | JumpReLU SAE collapses dense (L0≈48 regardless of λ) — STE bandwidth too small | high | fixed | sae-frontier: STE_BANDWIDTH 1e-3→0.1 + θ init 0.001→0.1; θ was gradient-starved on O(1) acts → never sparsified; corrected frontier has TopK leading not JumpReLU |
| 2026-07-02 | 2026-07-02-02 | S2 GPT-2 activation harvest crashes — tokenizer has no pad token | med | fixed | sae-frontier: set tok.pad_token=eos in activations.py (both harvest + loss-recovered) |
| 2026-07-02 | 2026-07-02-03 | Pre-push validation gate dormant — git-lfs hook holds .git/hooks/pre-push, core.hooksPath unset | med | fixed | FIXED 2026-07-03: rewrote .githooks/pre-push to chain git lfs pre-push + auto-detect a working python (dodges git-bash Store stub), set core.hooksPath=.githooks; git-lfs-install can no longer clobber the gate; todo fix-prepush-hook-wiring closed |
| 2026-07-02 | 2026-07-02-04 | MI-RIS handoff todo mis-stated EAP-IG's IOI faithfulness result (memory drift vs Hanna 2024) | med | fixed | handoff said "EAP-IG lifts IOI from ~0%"; paper §4.3: IOI EAP≈EAP-IG≈0.6, the ~0% catastrophe is SVA not IOI; corrected todo + grounded A2 hypotheses in the acquired PDF |
| 2026-07-02 | 2026-07-02-05 | Induction task had a fixed-offset positional shortcut — a 1-layer model solved it, defeating H1 | high | fixed | fixed-offset repeat let a 1-layer positional head copy without content-matching (§C.10 confound); rewrote make_induction_batch to a variable per-sequence offset; H1 restored (2L 0.982 vs 1L 0.145); regression test added |
| 2026-07-03 | 2026-07-03-01 | MPS cost profiling reports EAP wall-time > EAP-IG (impossible by op-count) | med | fixed | H15 study: 1-rep MPS timing lets the first-timed method eat a one-time scheduling cost; report cost by op-count (3/11/157 passes), not wall-time |
| 2026-08-13 | 2026-08-13-01 | run_phase3.py resume key ignores CFG — a reduced-config seed is silently reused as a full-config one | high | open | resume keys on (n_layers, seed) only, so editing CFG reduced→full skips already-trained seeds; aggregate() then stamps the pooled summary with the CURRENT config while some seeds were trained at the old one. Fires on exactly the reduced→full-scale transition the study defers to a GPU host. Each seed's own config IS on disk and never read back |
| 2026-07-03 | 2026-07-03-02 | Faithfulness significance pseudo-replicated (pooled n=160 p=5e-29 vs seed-level n=5 p=0.02) | med | fixed | H15 study: circuit fixed per seed, so per-example pooling inflates significance ~27 orders; gate on seed-level paired test; caught by pre-signoff adversarial audit; verdict PASS unaffected |
| 2026-08-14 | 2026-08-14-01 | RLEF citation in §8.3 misreads a valid/test pair as a before/after pair | high | fixed | two-column results table (Valid｜Test) read as a one-dimensional before/after sequence; 40.4 and 41.2 appear nowhere in the source, and the "38.0 public-tests-only" ablation does not exist. Real gain is 25.9→37.5 valid / 27.5→40.1 test at 1@3, ~11.6 pts not ~2.9. A substring check would PASS — what is wrong is the row/column assignment |
| 2026-08-14 | 2026-08-14-02 | study-frontier --since counts a pre-existing fold as new when only its paragraph anchor was renumbered | med | open | fold lines carry their `<a id="p-…">` anchor inline, so any renumber-paragraphs pass rewrites every fold line and the added-line scan re-counts them all. Turns the /study zero-folds honesty guardrail into a rubber stamp, exactly after routine survey maintenance. Fix = diff fold IDENTITY (para: ids / lead text), not line text |
| 2026-08-14 | 2026-08-14-03 | §4 attributes DeepSeek-R1's 96.3rd-percentile Codeforces result to the pure-RL recipe | high | fixed | R1-Zero (the SFT-free model) reaches 80.4; 96.3 is the production R1, which uses cold-start SFT + rejection-sampling SFT + a final RL stage mixing rule-based with MODEL-BASED preference rewards. So "rule-based rewards alone, no supervised traces" is wrong twice for that number. The survey's own §9 states it correctly — §4 cross-references §9 while contradicting it. Same wrong-axis-table class as 2026-08-14-01 |
| 2026-08-14 | 2026-08-14-04 | Six medium/low citation errors from the §§1-11/14-18 audit | med | fixed | CodeRL RL is over 5,000 APPS train problems not 10^4; Chinchilla's IsoFLOP row is 0.49/0.51 not 0.50/0.50; AlphaCode samples a MILLION not "thousands" (understating the section's own cost argument by 3 orders); Leviathan cited for a vendor latency figure it never measures; Qwen DPO falls back to an LLM judge (a gameable learned reward) for complex snippets; StarCoder frames FIM as going BEYOND completion. Finding 1 also required fixing the figure's .py/.json result-of-record artifacts + re-render |
| 2026-08-15 | 2026-08-15-01 | A workflow critic handed a sliced payload reports false "zero records" gaps, confidently | med | fixed | critique stage inlined `JSON.stringify(res).slice(0,15000)`; 4 of 6 critics then alleged papers returned no records and advised re-running ~12 agents. All 19 papers had records (86 total). The correlation is exact — every cluster alleging gaps exceeded the window; the only one that alleged none saw ~100%. A false-green INVERSION: a gate reporting failure that did not occur. Content critiques kept (one caught a 77%-vs-9% figure that would have shipped wrong); only the re-run verdicts discarded. Fix = pass the critic a file path, not an inline slice |
