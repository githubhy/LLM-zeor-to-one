# Conversation Log — 2026-06-17 — session: viewer-sync

## Conversation 1
- **Request**: "update the whole viewer from ../data-channel-receiver".
- **Actions**: Scouted both `viewer/` trees (29 files byte-identical; client/server + 4 lib files upstream-newer; upstream adds `vendor/`, `tests/`, `cloudflare/`, `publish.js`, `pull-annotations.js`, `GUIDE.md`, `playwright.config.js`, new tools, `lib/reading-position.js`, dep `ignore@^7`). Confirmed the viewer is content-agnostic (root discovered at runtime; citation URLs from git per-root) — only `package.json` carried repo identity. Diffed the 4 locally-larger Python tools: deltas are cosmetic LLM-retargeted docstrings/examples only (functional parity), so kept them. `rsync` synced everything else, excluding `node_modules/`, `test-results/`, `playwright-report/`, viewer `.claude/`, and those 4 tools. Adapted `viewer/package.json` (kept repo identity, adopted upstream scripts + `ignore` dep + playwright/wrangler devDeps). `npm install --omit=dev` (chokidar, ws, ignore; lockfile regenerated). Added `.viewerignore` at repo root + `viewer/test-results/`, `viewer/playwright-report/`, `viewer/dist/` to `.gitignore`. Retargeted `GUIDE.md` telecom example slugs → this repo's surveys. Filed decision `2026-06-17-01` and bug `2026-06-17-01`.
- **Result**: Viewer updated to upstream parity. Verified via a 6-arm `verify-viewer-sync` workflow (run `wf_78b6042c-f19`) + hand re-runs: python-tool tests 80/80, node-unit tests 286/286, adversarial review found no breakage, both survey gates GREEN (correct invocation), live boot/render confirmed. NOT committed (awaiting user go-ahead). One real defect surfaced (bug `2026-06-17-01`): a malformed `GET /api/md/<empty-or-dir>` crashes the dev server (unhandled EISDIR in upstream `serve.js`) — fix-location is the user's call (here / upstream+resync / defer).
- **Findings**: The synced dev `index.html` still loads KaTeX/markdown-it/mermaid from cdn.jsdelivr.net; `vendor/` is consumed by `publish.js` to bake the offline/Cloudflare bundle, not by the dev viewer — so GUIDE's "internet connection for CDN" note remains correct (verified before editing, avoided a wrong "fix"). My initial workflow gate script had two bugs (argv quoting; running `link-references.py` per body-file instead of per survey dir) that masqueraded as survey failures — the surveys are healthy; the authoritative per-`/check-survey` invocation is green.

## Conversation 2
- **Request**: (decision turn) chose how to handle bug `2026-06-17-01` (EISDIR server crash) → "Defer (file todo)".
- **Actions**: Filed `todos/2026-06-17-fix-serve-api-md-eisdir-crash.md` (+ `todos/INDEX.md`): fix upstream `serve.js` (isFile() guard + regression test), then re-sync to keep byte-convergence. Marked the bug record as deferred with a pointer to the todo. No code patched (preserves serve.js↔upstream parity).
- **Result**: Viewer sync turn fully closed; one open follow-up tracked. Still not committed — awaiting user go-ahead to commit/push.

## Conversation 3
- **Request**: Verify surveys/llms-for-coding appendix-a renders correctly and passes the full validation gate (render-gate check).
- **Actions**: Ran lint-math/renumber-equations/renumber-paragraphs per file, renumber-sections/link-references/validate-refs/bare-refs at survey scope; mechanically scanned appendix-a-qkv-first-principles.md for the five math-authoring hazards; boot-rendered the viewer on :4607 and curled the appendix API endpoint.
- **Result**: Gate fully GREEN (0 errors/warnings across all checks); appendix has 12 equations, 39 eligible paragraphs, all anchors/refs in sync. No rendering hazards found. HTTP 200 from /api/md/appendix-a-qkv-first-principles.md. PASS.
- **Findings**: File contains zero ordered-list markers, so the list/display-math failure modes are not applicable. Fraction text 1/16 and 1/24 sits inside `$...$` spans, so no digit-abut on the close delimiter.
## Conversation 4
- **Request**: "commit all and push" (the viewer sync), then "How do I understand the trained Q,K,V with first-principle math derivations".
- **Actions**: Committed the viewer sync (`04f2556`) — staged everything except the vestigial root `package*.json` — and pushed `0507245..04f2556 main`. Answered the Q/K/V question from first principles: the raw `W_Q,W_K,W_V` are gauge, not signal; only the QK circuit `M=W_QW_Kᵀ` (routing) and OV circuit `W_VW_O` (content) are observable, with kernel-regression + matched-filter intuitions, the `√d_k` variance argument, SVD reading, and induction heads. Flagged the circuit/induction source pointers as from-memory pending verification.
- **Result**: Viewer sync on `main`. Explanation delivered; user then asked to fold it into the survey (Conv 5).

## Conversation 5
- **Request**: Fold the Q/K/V derivation into the `llms-for-coding` survey appendix — no skipped steps, add diagrams, intuitions important.
- **Actions**: Verified sources (Vaswani PDF pp.3–5; WebFetched Elhage 2021 + Olsson 2022, verbatim quotes). Wrote **Appendix A** (`appendix-a-qkv-first-principles.md`, 12 sections A.1–A.12) cross-linked to section 3.3; built 4 numerical matplotlib figures (`qkv-sqrt-dk-scaling`, `qkv-gauge-invariance`, `qkv-lowrank-routing`, `qkv-induction-head` → SVG+JSON); added refs [59]/[60] (web, non-load-bearing); wired `order.json` + index Contents; ran the renumber/link/validate toolchain. Ran a 5-arm adversarial review workflow (`wf_7fba1273-8b7`); fixed every finding (Fig A.2 `1/16`→~0.25 caption; Eq 12 chain-rule bridge + intermediate steps; Eq 7 variance cancellation; Eq 8 wording; A.8 commit to SVD; A.5 kernel≠RBF; A.10 block-matrix identity; A.11 retitle + scope clarification).
- **Result**: Appendix complete and gate GREEN (lint 0, all `--check` clean, validate-refs 0/0, citation-sources 60/0). NOT committed — awaiting go-ahead. Decision `2026-06-17-02` (citation scoping) and field-note `2026-06-17-qkv-appendix` filed.
- **Findings**: Equations are numbered per-file (appendix restarts at tag 1). The figure-consistency reviewer caught a wrong caption value (`1/16`≈0.0625 vs the actual flat ~0.25) — a near-miss; the peak weight of a near-uniform softmax over 16 keys is ~0.25, not the uniform floor.
## Conversation 6
- **Request**: Apply math-symbol conventions — (1) column vectors, bold lowercase `\mathbf{a}`; (2) matrices non-bold capital; (3) scalars non-bold lowercase. Scope chosen: Appendix A + section 3.3.
- **Actions**: Rewrote `appendix-a-qkv-first-principles.md` in column+bold convention — the row→column flip makes `M = W_Q^⊤W_K` (QK) and `W_OV = W_OW_V` (OV), now matching Elhage et al. exactly (dropped the "up to convention" hedge). Flipped the two figure generators that form `M` (gauge, low-rank) to column convention + aligned all four docstrings; re-ran (only the gauge figure shifted: ~240% norm change, max|Δ|=3e-15). Bolded q,k,v in section 3.3 (+ `v_1,v_2,v_3`). Re-injected sec/para anchors, resynced link-references. Verified all 8 column-convention identities numerically (eq 3,4,5,8,9,12 incl. finite-difference gradient check) — ALL PASS. Saved feedback memory `math-notation-conventions`.
- **Result**: Gate GREEN (lint 0, all `--check` clean, validate-refs 0/0, citation-sources 60/0). NOT committed — awaiting go-ahead.
- **Findings**: Only `T` (sequence length) remains a capital scalar — kept for consistency with the survey body (section 3.1 uses `T`; lowercase `t` is the position index) and conventional usage; number of heads uses lowercase `h`. Flagged to user for veto.
## Conversation 7
- **Request**: Add diagrams to visualize the ideas in chapter A.2 (the query–key collapse).
- **Actions**: Built `figures/qkv-qk-collapse.py` (column convention) → new **Figure A.1** in A.2. *Left:* the T×T raw score matrix the bilinear form M produces, with the explicit q,k path matching to ~2e-14 (the collapse of eq 3 made literal). *Right:* scatter of `M_ab` vs `M_ba` showing M is generally asymmetric (ratio ≈ 1) → directed comparison; rank(M)=d_k. Chose the collapse + asymmetry as the A.2-specific story (rank/SVD is owned by the low-rank figure, gauge by the gauge figure). Renumbered the four existing figures (gauge→A.2, sqrt-dk→A.3, low-rank→A.4, induction→A.5) and the one prose ref; re-wired sec/para anchors + link-references.
- **Result**: Gate GREEN (lint 0, all `--check` clean, validate-refs 0/0 with 9 images, citation-sources 60/0); figure labels A.1–A.5 contiguous. NOT committed — awaiting go-ahead.
## Conversation 8
- **Request**: Add the missing steps between equation (1) and (2) in Appendix A.
- **Actions**: Expanded the eq (1)→(2) bridge in A.1 into the explicit entrywise unpacking: (i) rows of Q,K are the per-token vectors so `(QK^T)_ij = q_i^T k_j` → `s_ij`; (ii) row-wise softmax + causal mask (j>i → −∞, weight 0) → `a_ij`; (iii) the `·V` product reads row i as `o_i = Σ_{j≤i} a_ij v_j`. Prose-only (no new numbered equation, no renumber cascade).
- **Result**: Gate GREEN (lint 0, paragraphs `--check` clean, 12 equations sequential, validate-refs 0/0). NOT committed.
## Conversation 9
- **Request**: Add diagrams to visualize the ideas in A.5 (attention as kernel regression with a learned metric).
- **Actions**: Built `figures/qkv-kernel-regression.py` (column convention, M symmetric-PD) → new **Figure A.3** in A.5. *Left:* a 2-D feature plane, tokens colored by value response, attention weights as marker size for a query, output `o = Σ a_j v_j = 2.23` vs plain mean −0.12 (the kernel-weighted average of eq 6); weight grows along the `M x_q` direction (alignment, not proximity). *Right:* the attention kernel `exp(x_q^T M x/√d_k)` rises monotonically along the alignment ray while a Gaussian RBF peaks at the query and decays — the RBF caveat made visual. Renumbered the three later figures (sqrt-dk→A.4, low-rank→A.5, induction→A.6) and the one cross-ref; re-wired anchors + link-references.
- **Result**: Gate GREEN (lint 0, all `--check` clean, validate-refs 0/0 with 10 images, citation-sources 60/0); figure labels A.1–A.6 contiguous. NOT committed. (Note: a user-added blue highlight now sits in the A.5 prose — left intact.)
## Conversation 10
- **Request**: "fold these first-principle understandings in a wiki ... making an overwhelming comparison between these methods with the very basic kernel regression" — pointing at an iCloud ChatGPT export (`Kernel-Regression-Explanation.md`) that derives Nadaraya–Watson kernel regression, k-NN, Gaussian processes, RBF networks, and attention from first principles.
- **Actions**: Asked two scoping questions; user chose a new **Appendix B** inside `surveys/llms-for-coding/` and self-contained derivations reusing only existing refs 54/59/60. Wrote `appendix-b-kernel-regression-family.md` (8 sections B.1–B.8, eight numbered equations): the common linear-smoother skeleton + five comparison axes; Nadaraya–Watson from the conditional-expectation density ratio; k-NN, GPs, RBF networks each as a single edit to that form; attention (section B.6) as the headline member with four named departures, each cross-linked to Appendix A (sections A.1–A.9); a 6-row master comparison table + reductions lattice + synthesis. Built two computed matplotlib figures — `kernel-family-fits` (one 40-point dataset → NW/k-NN/GP-with-band/RBF fits, RMSE 0.208/0.192/0.198/0.165) and `kernel-family-weights` (equivalent-kernel weight vectors at one query: NW/k-NN non-negative summing to 1; GP/RBF signed with negative side-lobes, sums 0.996/1.005). Wired `order.json` + `index.md` (item 20). Ran the full toolchain. Ran a 5-lens adversarial-review workflow (math, figure↔data, table, Appendix-A consistency, citation/rendering) and fixed all five nits it surfaced (y_i→y_j index slip in B.6; softened "monotonically" to match A.5's SPD hedge; noted the unnormalized-KDE constant cancels; k-NN support-cell wording; bumped Figure B.2 panel-title sums to 3 dp so 0.996/1.005 show).
- **Result**: Appendix B complete and gate GREEN (lint 0, 8 equations sequential, 12 images valid, 43 cross-file links valid, bare-refs clean, citation-sources 60/0); review found no blocking/major issues. NOT committed — awaiting go-ahead (working tree also still holds the earlier uncommitted Figure A.3 + blue highlight).
- **Findings**: Every family member is the one linear smoother ŷ(x)=Σ wᵢ(x)·yᵢ; basic kernel regression is the non-negative + normalized + local + fixed-isotropic-kernel member, and each relative relaxes exactly one or two of those — the unifying spine of the comparison. GP and RBF equivalent-kernel weights empirically carry small negative side-lobes (min −0.04/−0.05) and do not sum to one (0.996/1.005), the concrete signature of relaxing the convex-average constraint.
## Conversation 11
- **Request**: Give concrete examples of how large the dimensions (d, T, d_v) are in real-world LLMs, smallest to largest — and fold the answer using the `survey-explainer-fold` skill from `../data-channel-receiver`, **adapting the skill into this repo first**.
- **Actions**:
  1. **Adapted the skill** → `.claude/skills/survey-explainer-fold/SKILL.md` (retargeted example paths to `surveys/llms-for-coding/`; rewrote the placement rule for this repo's link-targetable `A.N`/`B.N` headings vs 5G-NR lettered `G.1(e)` parts; updated the sweep to this repo's `viewer/tools/` invocations + a `check-citation-sources` step + a load-bearing "every concrete number must be read from an acquired source" clause). Registered it in `CLAUDE.md`'s Available Skills.
  2. **Citation-integrity evidence**: ran an 11-agent extraction workflow reading each acquired architecture PDF (Vaswani, phi-1, InCoder, CodeGen, Codex, Code Llama, StarCoder, StarCoder2, DeepSeek-Coder, Qwen2.5-Coder, DeepSeek-Coder-V2) for exact hidden-size / layers / heads / head-dim / context with page locators + verbatim quotes. Agents flagged computed-vs-stated values and correctly refused to backfill the three papers (Codex, Code Llama, DeepSeek-Coder-V2) that report only params + context.
  3. **Followed the skill**: a compact `> **Note —**` at the end of Appendix A section A.1 (where d, T, d_v are defined) summarizing the ranges with a forward link; a dedicated **section A.13 "Concrete Dimensions in Real-World Models"** appended at the end of Appendix A (cascade-free, after the Summary), holding a sourced 12-row smallest-to-largest table (d 512→7168, T —/2048→131072, d_k=d_v 64–128 with CodeGen's 256 outlier, h 8→56) plus reading / takeaways / intuition tying back to the 1/sqrt(d_k) scaling (A.7), the multi-head sum (A.10), and the kernel-regression / matched-filter readings (A.5/A.6). Every number cited to its existing reference; daggers mark computed values; no new references added.
  4. **Sweep**: fixed one lint block (NUMBER$\dagger$ digit-abut → plain Unicode †), then sections/paragraphs `--init` → full `--check` suite.
- **Result**: Skill adapted + registered; fold complete and gate GREEN (lint 0, 12 equations sequential / no cascade, validate-refs 0/0, bare-refs clean, citation-sources 60/0). NOT committed — awaiting go-ahead.
- **Findings**: Across acquired code-LLM sources the per-head width d_k=d_v is nearly frozen (64–128) and the residual width d grew only ~14× while parameters grew ~500× — depth and head count carry the scaling, while context T is the runaway dimension (~64× via RoPE/YaRN extension). Modern models use MQA/GQA, so the multi-head sum's h counts query heads with shared key/value heads. Three widely-used papers report only params + context and inherit their remaining dims silently — a citation-integrity trap the extraction correctly avoided rather than importing dims from base-model papers.
## Conversation 12
- **Request**: Elegant diagrams to illustrate the ideas in Appendix A section A.10 (multi-head attention as a sum of low-rank circuits).
- **Actions**: Read A.10 + neighbors + repo figure conventions. Found A.10 is the only A.N section lacking its own figure, and a latent bug: the A.9 induction-head figure's landmark was mislabeled `sec:A.10-figure-a` (confirmed no inbound links) — corrected it to `sec:A.9-figure-a`, freeing the id for a genuine A.10 figure (renumber-sections --check clean). Launched a design workflow (3 lenses: linear-algebra / mechanistic-circuit / SP filter-bank, then judge + adversarial faithfulness audit). First run died on transient 529 Overloaded (all 4 agents); re-launched.
- **Result**: A.9 landmark fix landed and verified. The re-run design workflow returned a synthesized+audited 2-panel spec (Panel A: Eq.(10) fan-out/sum-back dataflow with the EXACT block identity witnessed numerically; Panel B: 8 independent rank-≤d_k QK circuits routing one shared stream differently, via an 8×8 TV-dissimilarity matrix, plus a conserved-budget tiling strip). Implemented as `figures/qkv-multihead-sum.py` → .svg + .json (deterministic; additivity max|diff| 1.42e-14, ranks all 8, off-diag dissim mean 0.638), embedded as **Figure A.7** in section A.10 (using the freed `sec:A.10-figure-a` landmark). Full /check-survey GREEN (lint 0, 12 equations sequential / no cascade — figure mints no equation, sections/paragraphs/link-refs/validate-refs clean, bare-refs exit 0, citation-sources 60/0). NOT committed — awaiting go-ahead.
- **Findings**: The figure-landmark markers (`sec:A.N-figure-a`) are hand-authored, not auto-derived from the caption number (`-a` ≠ caption "A.5"), so a genuine A.10 figure required first correcting A.9's squatted id. The faithfulness audit rejected the "near-orthogonal subspaces" framing (a random-Gaussian dimension-counting fact, closest to the forbidden orthogonality overstatement) in favor of the behavioral TV-routing-dissimilarity matrix with explicit "independent, not orthogonal/disjoint" guards.

## Conversation 13
- **Request**: Enrich equation (11) — the softmax Jacobian in A.11 — to a multi-line, first-principles derivation with no intermediate step missing.
- **Actions**: Expanded the lead-in to define the row normalizer Z_i and state the two elementary derivatives (chain rule on the exponential giving Kronecker delta_jm; only the m-th term of Z_i carries s_im), then replaced the one-line eq (11) with a 6-step `\begin{aligned}...\end{aligned} \tag{11}` block (quotient rule, substitute, split/cancel, recognize softmax weights, factor), each line annotated with `&&\text{(justification)}` per the corpus pattern. Dropped the now-redundant "delta_jm the Kronecker delta" clause from the following paragraph.
- **Result**: Green — equations --check 12 tags sequential (no cascade, eq 11 stays eq 11), sections/paragraphs --check clean, validate-refs 0/0, bare-refs exit 0, lint-math accepted the aligned block.
- **Findings**: The corpus already uses `\begin{aligned}...\end{aligned} \tag{N}` (with `&&\text{}` right-side step annotations) exactly once (language-models-from-first-principles.md eq 2); reusing that placement is the safe KaTeX + linter-clean way to author multi-line display math here.

## Conversation 14
- **Request**: Commit and push (the Figure A.7 work + the earlier eq (11) enrichment).
- **Actions**: Staged `appendix-a-qkv-first-principles.md`, `figures/qkv-multihead-sum.{py,svg,json}`, and the session log (root `package*.json` left untracked); committed `cb2af33`; pushed `2c69614..cb2af33 main` (dangerouslyDisableSandbox to bypass the sandbox SSH block).
- **Result**: Pushed; /check-survey was green at commit time.

## Conversation 15
- **Request**: Make the "enrich an equation to a multi-line, first-principles, no-intermediate-step-missing derivation" procedure a reusable command.
- **Actions**: Created `.claude/commands/enrich-equation.md` (frontmatter description + argument-hint; 6 steps — locate equation + its foundations → rewrite lead-in to make each step one rule → replace with a same-`\tag` multi-line `aligned` block, one annotated move per line → delete now-redundant prose → validation sweep → log; plus a Rules section codifying no-new-tags/no-cascade, KaTeX-safe macros, blank-line-after-`$$`, digit-abut, and marked-ref-only guardrails; worked example = the eq (11) softmax-Jacobian expansion). Registered it in `CLAUDE.md`'s Commands list.
- **Result**: `/enrich-equation <file> (<N>)` is discoverable and ready. NOT committed — awaiting go-ahead.
- **Findings**: The `post-edit-lint.sh` PostToolUse hook explicitly skips the `.claude/` directory (case match, exit 0), so command files may contain example `$$`/`\tag{N}`/`<!-- eq: -->` snippets without being linted, blocked, or auto-renumbered.

## Conversation 16
- **Request**: Bug — highlighting `$\delta_{jm}$ the Kronecker delta (1 when $j=m$, else 0)` in appendix A section A.11 (the equation-(11) lead-in) highlights the whole paragraph.
- **Actions**: Traced the viewer highlight classifier (`applyHighlight` in `viewer/viewer.js`). Root cause: a selection that starts *inside* an inline-math span and ends in plain text *after* a second math span hits the `startKatex && !endKatex` branch with `nKatex>1`, which routed to the whole-block SIDECAR backend (`sidecarSegmentsFromRange` paints by source line → whole paragraph). That sidecar route was a deliberate workaround (bug 2026-06-02-04) to avoid a "Could not locate" toast, but it used the single-math handler's reasoning. Fixed by routing the case to the multi-span `PLAIN_SPANNING_MATH` handler (Step 5P), which reconstructs a precise source span across every spanned math and collapses `plainHead` to empty when the range starts inside the first katex (end-before-start rule) → `selStart` anchors at that math's source offset. Verified empirically with Playwright (installed `@playwright/test` + matching headless-shell, `--no-save`): precise inline `==color==` highlight, no error toast, no sidecar entry, no page errors. Repurposed the bug-2026-06-02-04 regression test to assert the precise behavior; the 7-span Step 5P test still passes; the broader `highlights` / `highlight-color-prefix-rendering` suites pass (exit 0). Filed bug `2026-06-19-01` (+ bugs/INDEX) and todo `2026-06-19-port-multispan-highlight-fix-upstream` (the identical branch is buggy in `../data-channel-receiver`; port per the convergence policy, decision 2026-06-17-01).
- **Result**: Fix landed in `viewer/viewer.js` + regression test green, no regressions. NOT committed — awaiting go-ahead. `surveys/llms-for-coding/.viewer-highlights/appendix-a-...json` is an empty runtime sidecar doc (untracked, not gitignored) — left as-is; `.viewer-highlights/` likely warrants a gitignore entry.
- **Findings**: `PLAIN_SPANNING_MATH` already handled the start-inside-katex case all along; the prior sidecar workaround reached for the wrong handler (Step 5M, single-math) and so fell back to whole-block instead of routing to the multi-span Step 5P. The `post-edit-lint.sh` hook lints `prompts/` and `bugs/` (it only skips `.claude/`), so records in those dirs must avoid bare dotted section refs (caught one such ref in the bug file on first write).

## Conversation 17
- **Request**: `/enrich-equation (12)` — enrich equation (12) into a multi-line, first-principles, no-step-missing derivation.
- **Actions**: First end-to-end run of the new `/enrich-equation` command, on equation (12) (the score-gradient identity in appendix A section A.11). Rewrote the lead-in (para 2) to state the two ingredients — the weight-to-loss gradient (chain rule through the output, definition (2)) and the softmax Jacobian (equation (11)) — each with a marked ref. Expanded the compressed four-equals chain into a six-step `aligned` block: chain rule via the weights, insert both ingredients, distribute, Kronecker-delta selects the matching key and the per-key weight is pulled out, factor out the common gradient-weight factor, collapse the value-average to the output; each line annotated, same `\tag{12}`. Removed the now-redundant "the middle steps are…" sentence from para 3 (its content is now the per-step annotations).
- **Result**: Gate green — 12 equations sequential / no cascade, sections/paragraphs/link-refs/validate-refs clean, bare-refs 0, citation-sources 60/0, lint-math accepted the aligned block. NOT committed (command run; awaiting go-ahead).
- **Findings**: The command's discipline transferred cleanly to a different equation shape (a compressed multi-equals chain, vs equation (11)'s one-liner) — same-tag aligned, marked-ref-only in prose, rule-named annotations inside the block.

## Conversation 18
- **Request**: "merge this to the multi-line too" — promote the first ingredient of the score-gradient derivation (the chain rule giving the weight-to-loss gradient, previously a one-line prose assertion in the A.11 lead-in) to its own multi-line first-principles derivation, like the score-gradient equation.
- **Actions**: Established that a rendered multi-line block must be a numbered equation here — scanned the corpus (31 display blocks, all tagged; zero untagged) and confirmed lint-math check #9 flags untagged displays. Promoted the first ingredient to a new equation (12): a four-step chain rule (scalar weight through the vector output's components, the per-component derivative, reassemble the inner product, identify the output-gradient), each line annotated. Split the lead-in into an ingredient-1 paragraph (before the new equation) and an ingredient-2 paragraph (the softmax Jacobian, equation (11)); the former score-gradient equation became (13) (ID A-13, anchor eq-13). One atomic edit produced the final structure (no duplicate-anchor transient for the auto-hook); updated the lone downstream reference (the A.12 summary) to (13); renumber-paragraphs --init renumbered the inserted paragraph (the section's later para shifted cleanly, no inbound refs).
- **Result**: Gate green — 13 equations sequential / no orphans, sections/paragraphs/link-refs/validate-refs clean, bare-refs 0, citation-sources 60/0, lint-math clean. NOT committed (awaiting go-ahead).
- **Findings**: The repo has no untagged-display escape hatch (lint-math warns), so a multi-line "helper" derivation must become a numbered equation; the stable-ID marker system made the insert plus the tiny downstream renumber (equation 12→13, one reference) mechanical, and doing it as one atomic edit kept the auto-renumber hook from mangling a transient duplicate.

## Conversation 19
- **Request**: Elegant diagrams to illustrate the section A.11 co-adaptation paragraph (para -4): the sign rule (gradient descent raises s_im when delta_i^T(v_m - o_i) < 0), QK/OV co-adaptation ("where to look" vs "what to bring", each conditioned on the other), and the caveat that this does not prove convergence to the A.8–A.9 circuits.
- **Actions**: section A.11 had no figure. Ran a 3-lens design workflow (geometric half-space / co-adaptation feedback-loop / SP error-projection) → judge + adversarial faithfulness audit. The audit agent and the SP lens died on transient socket errors, but the two surviving proposals self-audited and converged on the same risk list, so I synthesized the merge inline and adopted the most faithful choices. Built **Figure A.8** (`figures/qkv-coadaptation.py` → svg + json, deterministic): Panel A = schematic training loop (M where-to-look → pattern → W_OV what-to-bring → o_i → L → delta_i splits into two teaching arrows back into M and W_OV; dashed "(M, W_OV) pair, never the six raw matrices" box; A.6 closed-form-not-LMS footnote); Panel B = computed half-space sign rule (hyperplane through o_i perpendicular to delta_i splits five value points into attend-more green d<0 / attend-less red d>0; verified a=[0.28,0.14,0.34,0.08,0.17], o_i=(0.75,0.36), signed dots d_j=[+0.63,-3.02,+1.85,-1.51,-1.53]); shared caveat banner (co-adaptation, not convergence; builds neither the A.8 routing nor the A.9 copy/induction circuit). Embedded after para -4 at the freed `sec:A.11-figure-a` landmark; visual-QA'd via a temp PNG (fixed a hyperplane label that landed off-panel).
- **Result**: Figure A.8 embedded; gate green — 13 equations sequential / no cascade (figure mints no equation), sections/paragraphs/link-refs/validate-refs clean, bare-refs 0, citation-sources 60/0, lint-math 0. NOT committed (awaiting go-ahead).
- **Findings**: Workflow design agents inherit the repo CLAUDE.md, so a design workflow leaves side-effects to clean up: one agent wrote a prototype script straight into the real `figures/` dir (overwritten by my vetted version), and another created its own `prompts/` session log (removed). The audit's decisive call: drop the prototype's stand-in quadratic loss + one-step-descent (a didactic add-on) in favor of the pure abstract-delta_i half-space, which needs no invented loss and matches exactly what A.11 gives (delta_i = dL/do_i only).

## Conversation 20
- **Request**: Build the recommended 2-panel Figure 3.5 for section 3.5 (training + serving economics), sourcing the training-stage budgets.
- **Actions**: Ran a 5-PDF extraction workflow (agents told NOT to write files) for per-stage training-data sizes with verbatim quotes + page locators. Sourced numbers, all STATED in tokens: DeepSeek-Coder pretraining 2T / SFT 2B [10]; Qwen2.5-Coder 5.5T [11]; StarCoder2 3.3–4.3T [9]; Code Llama ~5B instruction tokens [6]; CodeRL RL over ~10^4 APPS problems [19]. Built Figure 3.5 (figures/training-and-serving-economics.py → svg + json, deterministic): Panel A = log-scale training tokens per stage (DeepSeek-Coder 2T pretrain vs 2B SFT = 1000×, since at fixed N tokens equal compute; faint cross-model reference dots; honest alignment annotation — RL/preference rarely token-quantified, CodeRL ≈ 10^4 problems not tokens); Panel B = analytical KV-cache cost, cumulative block-passes uncached T(T+1)/2 = O(T^2) vs cached T = O(T), log-log, annotated ~4097× at T=8192. Embedded at the end of section 3.5 as a plain bold-caption figure (this file's convention — no sec-figure landmark), citing all five refs + the cross-entropy equation (2). Visual-QA'd via temp PNG (fixed an alignment annotation that collided with the x-tick).
- **Result**: Figure 3.5 embedded; gate green — 10 equations sequential, sections/paragraphs clean, link-references up to date (5 new citations normalized to house format), validate-refs 43/43, bare-refs 0, citation-sources 60/0, lint-math 0. NOT committed (awaiting go-ahead).
- **Findings**: The alignment stage is a systematic sourcing gap — open code-LLM papers report pretraining (and often SFT) in tokens but rarely quantify RL/preference data in tokens at all (DeepSeek-Coder has no RL stage; Qwen2.5-Coder's DPO data unreported; CodeRL counts problems, not tokens). The faithful move is to plot pretrain→SFT quantitatively and represent alignment as "smaller still, rarely token-quantified" rather than fabricate a value. Operationally: telling the extraction agents "do not write files / do not create logs" cleanly prevented the prototype-script and stray-prompts-log side-effects that the earlier design workflows left behind.

## Conversation 21
- **Request**: Fold the KV-cache "why O(T), not O(T^2)" explanation into section 3.5, adding illustrative diagrams.
- **Actions**: Used the survey-explainer-fold pattern. (1) Inline `> **Note —**` at the section-3.5 serving host (after the generation-loop mermaid): one-paragraph answer (causal invariance + the sum-of-t count) with a forward link to subsection 3.5.1. (2) New dedicated subsection 3.5.1 "Why the KV Cache Makes Generation Linear, Not Quadratic" (the first 4-level #### heading in section 3) in answer-format: the count (cacheless sum_{t=1}^T t = T(T+1)/2 = O(T^2) vs cached T = O(T)); causal invariance (key/value at position j depend only on tokens <= j, so appending a token freezes them — caching is lossless); the honest caveat (the attention scan is O(t) per token either way; the cache removes the prefix recomputation); and an SP intuition (recursive state vs from-scratch recompute). (3) Built Figure 3.6 (figures/kv-cache-mechanism.py → svg + json): Panel A = work tableau (cacheless lower-triangle = 36 = T(T+1)/2 vs cache diagonal = 8 = T, at T=8); Panel B = computed causal invariance through a 2-layer causal stack (layer-2 prefix keys bit-identical whether recomputed alone or inside a longer sequence, max key change = 0.0 exactly). Inline math only (no new numbered equation → no cascade). Both links wired. Built the figure directly with adversarial self-check (clear, narrow design) rather than a design workflow.
- **Result**: Fold complete + Figure 3.6 embedded; gate green — 3.5.1 anchored, 10 equations sequential / no cascade, sections/paragraphs/link-refs/validate-refs clean, bare-refs 0, citation-sources 60/0, lint-math 0. NOT committed (also still uncommitted from Conversation 20: Figure 3.5).
- **Findings**: A blockquote (or any paragraph) must not begin its first line with a `<!-- secref/cite/ref -->` marker — CommonMark parses a column-0 comment as an HTML block and swallows the line (lint-math caught it at write time); lead with prose. Also: the 3.5.1 deep-dive is the first 4-level heading in section 3, and renumber-sections handles 3-level digit section numbers cleanly.

## Conversation 22
- **Request**: Make new chapters: a deep, first-principles anatomy of LLM structure from the most basic LLM to the largest — top-structure to single-neuron, end-to-end math (no step missing), deep intuition, many diagrams. Then: "go end-to-end automatically unless you genuinely need my help."
- **Actions**: Planned it; user settled four forks (appendix series here; scale-primary; forward+backward+optimizer; include frontier models). Set up tasks #25–#32, wrote + committed the plan (`plans/2026-06-19-llm-anatomy-appendix-series.md`) and decision (`2026-06-19-01`), and committed the two prior gated-green section-3.5 figures + the 3.5.1 fold that were still pending (commits 4922520, 76ba9af, pushed). Then built **Appendix C — the calibration chapter** ("A Transformer Top-to-Neuron: a Fully Worked Toy Model"): a tiny decoder (V=3, d=4, T=3, 1 head, d_ff=8, 1 block) with the COMPLETE forward, COMPLETE backward (every gradient), and one Adam step derived from first principles (10 numbered equations, C.1–C.6). Correctness backbone: `figures/appendix-c-toy-transformer.py` implements forward+backward+Adam and runs a central finite-difference gradient check on all 203 params — max relative error 1.6e-9 (asserted) — so the chapter's math is verified, not asserted; it also trains the toy (loss 1.06 → 0.78 one step → 0.0056 over 60 Adam steps). Two figures: the top-to-neuron anatomy zoom (`appendix-c-anatomy.py`) and the gradient-check + training-curve validation. Wired order.json + index.md; full sweep green.
- **Result**: Appendix C complete and gate-green (sections/paragraphs/link-refs/validate-refs clean, 10 equations sequential, bare-refs 0, citation-sources 60/0, lint 0). Committed + pushed. Tasks #25, #27 done; #26 (sourcing) + #28–#32 (chapters D–H) remain — continuing.
- **Findings**: For a multi-equation chapter, implementing the model + a finite-difference gradient check FIRST (then writing the derivations the check validated) is the way to guarantee "no step missing" is also "no step wrong." Also: composing long markdown with hard-wrapped paragraphs is dangerous — a wrapped line starting with `<!--` or `-` trips lint (HTML-block / list-marker); keep survey paragraphs one physical line.

## Conversation 23
- **Request**: Continuation of the anatomy series under "keep going, commit each chapter as you finish" — build Appendices D and E.
- **Actions**: Shipped **Appendix D — GPT-2 Scale** (`appendix-d-gpt2.md`, commit e286f2c): instantiates Appendix C's anatomy at GPT-2's four sourced sizes [61], deriving only the deltas — GELU (forward + exact derivative Φ+xφ), tied unembedding W_U=E^T (gradient = sum of read+write paths), the parameter formula N≈(V+T_ctx)d+L·12d² (computed 124M–1.56B vs paper 117M–1542M, counting-convention note), and C≈6ND. Figures D.1 (sourced sizes + param-budget collapse 32%→5%) and D.2 (GELU vs ReLU + derivatives). Then **Appendix E — the modern dense block** (`appendix-e-modern-dense.md`, commit b6c42a5): the four Llama substitutions, each forward AND backward, every gradient finite-difference-checked first via `figures/appendix-e-validate.py` (RMSNorm dx/dg 4e-10; SwiGLU all <3e-9; RoPE relative-position identity 9e-16; GQA=MHA at G=H exactly). E.1 sourced sizes [65, Table 2], E.2 RMSNorm, E.3 RoPE, E.4 SwiGLU (8/3 d derived as param-match), E.5 GQA + KV-cache. Four figures (block ledger, RoPE rotation/offset, SwiGLU gate, GQA head-sharing/cache). Acquired `download/llama1-2023.pdf` [65] (direct arXiv, resolver-bypassed) + registered [66] RoFormer; both verified page-1 before citing. Both chapters wired into order.json + index.md; full sweep green (0 errors) on each; pushed.
- **Result**: Appendices C–E done (tasks #27–#29 closed); #30 (Appendix F, 33B–70B scaling) in progress. Series continues autonomously.
- **Findings**: matplotlib mathtext rejects `\tfrac`/`\dfrac` (same class as `\big`/`\lVert`) — use `\frac`. Cross-file equation references have no marker form in this corpus (only `secref`/`secxref` for sections); reference the owning *section*, never another file's `eq-N` via a same-file `ref:` marker (it orphans). A `secxref:ID` marker must match its anchor's full section number (`secxref:3.5.1`, not `3.5`) or renumber-sections rewrites the visible link to the wrong target. Bare `[N, locator]` citations don't convert — use the explicit `<!-- cite:N -->[[N]](references.md#ref-N)` marked form with the locator as adjacent prose. qlmanage crops wide SVGs in QA; `rsvg-convert` renders faithfully.

## Conversation 24
- **Request**: Finish the anatomy series — Appendices F, G, H — under the standing "keep going, commit each chapter as you finish."
- **Actions**: Shipped three chapters, each gated green and pushed. **Appendix F — Scaling the Dense Block 33B–70B** (`appendix-f-scaling.md`, commit 742e7d0): thesis that scale changes only (d,H,L); the d/L aspect ratio (~100–128); the memory wall derived from Adam (16N bytes train vs 2N infer → 70B ≈1.12 TB → sharded); GQA's KV-cache payoff at 100k context (MHA 262 GB vs GQA 33 GB); the dense ceiling. Figures F.1 (scale ladder) + F.2 (memory wall). **Appendix G — Frontier MoE / DeepSeek-V3** (`appendix-g-moe.md`, commit f575a07): the router forward (sigmoid affinity, top-Kr, normalized gating; DeepSeek-V3 Eqs 12–15) + the router BACKWARD gradient-checked first via `figures/appendix-g-validate.py` (du 1.6e-9, centroids dE 2e-8); auxiliary-loss-free load balancing; the capacity/compute decoupling (671B total / 37B active); MLA cache (~57× vs MHA). Figures G.1 (dense-vs-MoE FFN), G.2 (decoupling), G.3 (routing collapse vs balanced). **Appendix H — Synthesis** (`appendix-h-synthesis.md`, commit ca2a43f): master table of all dims across the 5 models (toy→GPT-2→7B→70B→DeepSeek-V3, all sourced [61,65,63,64]); the five-axis accretion narrative; the scaling-account reference card; the invariant core; reading the ladder for code. Figures H.1 (architecture-choices matrix) + H.2 (scale sweep 203→671B params, 3→128K ctx). All DeepSeek-V3 facts verified from the PDF (L=61, d=7168, 1 shared+256 routed, K_r=8, MLA d_c=512, 128K vocab/context); DeepSeek-Coder-V2 [43] verified (236B/21B, 16B/2.4B).
- **Result**: **Full LLM-anatomy series C–H complete** (tasks #27–#32 all closed). Final whole-survey delivery gate green across all 26 files: renumber {sections,equations,paragraphs} --check clean, link-references up to date, validate-refs 0/0 (113 .md links valid), bare-refs --severity=error exit 0, citation-sources 66/66 (all cited, 0 errors), lint-math 0 across C–H. 6 new chapters, ~24 numbered equations, 14 figures, 2 new sources ([65] Llama-1, [66] RoFormer). Remaining optional sign-off: the `citation-audit` skill (all content main-thread-authored against page-1-verified PDFs, so low-risk, but it is the documented final gate).
- **Findings**: For the MoE router backward, the gate gradient ds_k = (1/Z)(dg_k − Σ_i g_i dg_i) is the softmax-renormalization coupling even though selection uses sigmoid affinities + top-k (top-k itself carries no gradient — it is a mask). DeepSeek-V3's auxiliary-loss-free balancing is elegant precisely because the bias b_i enters SELECTION but not the gate value, so it steers load without adding a loss term or distorting the combine weights. The engine-first discipline (validate the new module's gradient before writing its derivation) held for every chapter and caught nothing wrong only because the derivations were written from the validated code, not from memory.

## Conversation 25
- **Request**: "go" — run the formal `citation-audit` as the final sign-off gate on appendices C–H.
- **Actions**: Ran citation-audit Phases 1–6 orchestrated as a multi-agent workflow (`wf_1ce7741b-b4d`, 11 agents, 384k tokens): one verifier per source PDF (page-1 identity + locus-targeted claim/value reproduction) + an adversarial second reader on every non-correct claim. Ledger of 10 external refs (C has none); all 10 identities ✓; **24 of 26 claims `correct`**, every load-bearing DeepSeek-V3 number (671B/37B, 1+256 experts, top-8, d_c=512, d_h^R=64, L=61, d=7168, 128K vocab/ctx) and Llama-1 Table 2 reproduced exactly. One `wrong-value` (bug `2026-06-20-01`, low): E.5 attributed Llama-2-70B's 64-head count to [63], which states no per-model head count — reworded to "grouped-query attention — 8 key/value groups —" citing only what [63]'s text says. The H-table 70B-dims flag was a false alarm (dims trace to [65]'s 65B config). Fixed E.5, filed the bug + index row, wrote `reports/citation-audit-appendices-c-h-2026-06-20.md`, re-validated (0 errors), committed + pushed (4a4d950).
- **Result**: **C–H series passes the citation gate.** No fabricated citations, no wrong value in any derivation, all sources local + verified. The whole LLM-anatomy series is now drafted, gate-green, and citation-audited.
- **Findings**: The audit's one catch was an *attribution* defect with a *correct* value — exactly the failure the citation-integrity rule targets that a value-only check would miss: H=64 is true of Llama-2-70B but is derived/config, not in the [63] PDF text. The impact trace mattered: the same number is consumed by F.3's KV-cache derivation, but there it is derived from [65]-sourced dimensions, so no result moved. Verifier agents must distinguish "not in THIS paper" (`wrong-value`/`unverifiable` vs the cited source) from "false" — the H-table dims were `unverifiable` against [63] yet fully correct against [65], i.e. not a defect.

<!-- LOG-END -->
