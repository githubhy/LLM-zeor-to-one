# Conversation Log — 2026-06-17 — session: viewer-sync

## Conversation 1
- **Request**: "update the whole viewer from ../data-channel-receiver".
- **Actions**: Scouted both `viewer/` trees (29 files byte-identical; client/server + 4 lib files upstream-newer; upstream adds `vendor/`, `tests/`, `cloudflare/`, `publish.js`, `pull-annotations.js`, `GUIDE.md`, `playwright.config.js`, new tools, `lib/reading-position.js`, dep `ignore@^7`). Confirmed the viewer is content-agnostic (root discovered at runtime; citation URLs from git per-root) — only `package.json` carried repo identity. Diffed the 4 locally-larger Python tools: deltas are cosmetic LLM-retargeted docstrings/examples only (functional parity), so kept them. `rsync` synced everything else, excluding `node_modules/`, `test-results/`, `playwright-report/`, viewer `.claude/`, and those 4 tools. Adapted `viewer/package.json` (kept repo identity, adopted upstream scripts + `ignore` dep + playwright/wrangler devDeps). `npm install --omit=dev` (chokidar, ws, ignore; lockfile regenerated). Added `.viewerignore` at repo root + `viewer/test-results/`, `viewer/playwright-report/`, `viewer/dist/` to `.gitignore`. Retargeted `GUIDE.md` telecom example slugs → this repo's surveys. Filed decision `2026-06-17-01` and bug `2026-06-17-01`.
- **Result**: Viewer updated to upstream parity. Verified via a 6-arm `verify-viewer-sync` workflow (run `wf_78b6042c-f19`) + hand re-runs: python-tool tests 80/80, node-unit tests 286/286, adversarial review found no breakage, both survey gates GREEN (correct invocation), live boot/render confirmed. NOT committed (awaiting user go-ahead). One real defect surfaced (bug `2026-06-17-01`): a malformed `GET /api/md/<empty-or-dir>` crashes the dev server (unhandled EISDIR in upstream `serve.js`) — fix-location is the user's call (here / upstream+resync / defer).
- **Findings**: The synced dev `index.html` still loads KaTeX/markdown-it/mermaid from cdn.jsdelivr.net; `vendor/` is consumed by `publish.js` to bake the offline/Cloudflare bundle, not by the dev viewer — so GUIDE's "internet connection for CDN" note remains correct (verified before editing, avoided a wrong "fix"). My initial workflow gate script had two bugs (argv quoting; running `link-references.py` per body-file instead of per survey dir) that masqueraded as survey failures — the surveys are healthy; the authoritative per-`/check-survey` invocation is green.

## Conversation 2
- **Request**: (decision turn) chose how to handle bug `2026-06-17-01` (EISDIR server crash) → "Defer (file todo)".
- **Actions**: Filed `todos/2026-06-17-fix-serve-api-md-eisdir-crash.md` (+ `todos/INDEX.md`): fix upstream `serve.js` (isFile() guard + regression test), then re-sync to keep byte-convergence. Marked the bug record as deferred with a pointer to the todo. No code patched (preserves serve.js↔upstream parity).
- **Result**: Viewer sync turn fully closed; one open follow-up tracked. Still not committed — awaiting user go-ahead to commit/push.

## Conversation 3
- **Request**: Verify surveys/llms-for-coding appendix-a renders correctly and passes the full validation gate (render-gate check).
- **Actions**: Ran lint-math/renumber-equations/renumber-paragraphs per file, renumber-sections/link-references/validate-refs/bare-refs at survey scope; mechanically scanned appendix-a-qkv-first-principles.md for the five math-authoring hazards; boot-rendered the viewer on :4607 and curled the appendix API endpoint.
- **Result**: Gate fully GREEN (0 errors/warnings across all checks); appendix has 12 equations, 39 eligible paragraphs, all anchors/refs in sync. No rendering hazards found. HTTP 200 from /api/md/appendix-a-qkv-first-principles.md. PASS.
- **Findings**: File contains zero ordered-list markers, so the list/display-math failure modes are not applicable. Fraction text 1/16 and 1/24 sits inside `$...$` spans, so no digit-abut on the close delimiter.
## Conversation 4
- **Request**: "commit all and push" (the viewer sync), then "How do I understand the trained Q,K,V with first-principle math derivations".
- **Actions**: Committed the viewer sync (`04f2556`) — staged everything except the vestigial root `package*.json` — and pushed `0507245..04f2556 main`. Answered the Q/K/V question from first principles: the raw `W_Q,W_K,W_V` are gauge, not signal; only the QK circuit `M=W_QW_Kᵀ` (routing) and OV circuit `W_VW_O` (content) are observable, with kernel-regression + matched-filter intuitions, the `√d_k` variance argument, SVD reading, and induction heads. Flagged the circuit/induction source pointers as from-memory pending verification.
- **Result**: Viewer sync on `main`. Explanation delivered; user then asked to fold it into the survey (Conv 5).

## Conversation 5
- **Request**: Fold the Q/K/V derivation into the `llms-for-coding` survey appendix — no skipped steps, add diagrams, intuitions important.
- **Actions**: Verified sources (Vaswani PDF pp.3–5; WebFetched Elhage 2021 + Olsson 2022, verbatim quotes). Wrote **Appendix A** (`appendix-a-qkv-first-principles.md`, 12 sections A.1–A.12) cross-linked to section 3.3; built 4 numerical matplotlib figures (`qkv-sqrt-dk-scaling`, `qkv-gauge-invariance`, `qkv-lowrank-routing`, `qkv-induction-head` → SVG+JSON); added refs [59]/[60] (web, non-load-bearing); wired `order.json` + index Contents; ran the renumber/link/validate toolchain. Ran a 5-arm adversarial review workflow (`wf_7fba1273-8b7`); fixed every finding (Fig A.2 `1/16`→~0.25 caption; Eq 12 chain-rule bridge + intermediate steps; Eq 7 variance cancellation; Eq 8 wording; A.8 commit to SVD; A.5 kernel≠RBF; A.10 block-matrix identity; A.11 retitle + scope clarification).
- **Result**: Appendix complete and gate GREEN (lint 0, all `--check` clean, validate-refs 0/0, citation-sources 60/0). NOT committed — awaiting go-ahead. Decision `2026-06-17-02` (citation scoping) and field-note `2026-06-17-qkv-appendix` filed.
- **Findings**: Equations are numbered per-file (appendix restarts at tag 1). The figure-consistency reviewer caught a wrong caption value (`1/16`≈0.0625 vs the actual flat ~0.25) — a near-miss; the peak weight of a near-uniform softmax over 16 keys is ~0.25, not the uniform floor.
## Conversation 6
- **Request**: Apply math-symbol conventions — (1) column vectors, bold lowercase `\mathbf{a}`; (2) matrices non-bold capital; (3) scalars non-bold lowercase. Scope chosen: Appendix A + section 3.3.
- **Actions**: Rewrote `appendix-a-qkv-first-principles.md` in column+bold convention — the row→column flip makes `M = W_Q^⊤W_K` (QK) and `W_OV = W_OW_V` (OV), now matching Elhage et al. exactly (dropped the "up to convention" hedge). Flipped the two figure generators that form `M` (gauge, low-rank) to column convention + aligned all four docstrings; re-ran (only the gauge figure shifted: ~240% norm change, max|Δ|=3e-15). Bolded q,k,v in section 3.3 (+ `v_1,v_2,v_3`). Re-injected sec/para anchors, resynced link-references. Verified all 8 column-convention identities numerically (eq 3,4,5,8,9,12 incl. finite-difference gradient check) — ALL PASS. Saved feedback memory `math-notation-conventions`.
- **Result**: Gate GREEN (lint 0, all `--check` clean, validate-refs 0/0, citation-sources 60/0). NOT committed — awaiting go-ahead.
- **Findings**: Only `T` (sequence length) remains a capital scalar — kept for consistency with the survey body (section 3.1 uses `T`; lowercase `t` is the position index) and conventional usage; number of heads uses lowercase `h`. Flagged to user for veto.
## Conversation 7
- **Request**: Add diagrams to visualize the ideas in chapter A.2 (the query–key collapse).
- **Actions**: Built `figures/qkv-qk-collapse.py` (column convention) → new **Figure A.1** in A.2. *Left:* the T×T raw score matrix the bilinear form M produces, with the explicit q,k path matching to ~2e-14 (the collapse of eq 3 made literal). *Right:* scatter of `M_ab` vs `M_ba` showing M is generally asymmetric (ratio ≈ 1) → directed comparison; rank(M)=d_k. Chose the collapse + asymmetry as the A.2-specific story (rank/SVD is owned by the low-rank figure, gauge by the gauge figure). Renumbered the four existing figures (gauge→A.2, sqrt-dk→A.3, low-rank→A.4, induction→A.5) and the one prose ref; re-wired sec/para anchors + link-references.
- **Result**: Gate GREEN (lint 0, all `--check` clean, validate-refs 0/0 with 9 images, citation-sources 60/0); figure labels A.1–A.5 contiguous. NOT committed — awaiting go-ahead.
## Conversation 8
- **Request**: Add the missing steps between equation (1) and (2) in Appendix A.
- **Actions**: Expanded the eq (1)→(2) bridge in A.1 into the explicit entrywise unpacking: (i) rows of Q,K are the per-token vectors so `(QK^T)_ij = q_i^T k_j` → `s_ij`; (ii) row-wise softmax + causal mask (j>i → −∞, weight 0) → `a_ij`; (iii) the `·V` product reads row i as `o_i = Σ_{j≤i} a_ij v_j`. Prose-only (no new numbered equation, no renumber cascade).
- **Result**: Gate GREEN (lint 0, paragraphs `--check` clean, 12 equations sequential, validate-refs 0/0). NOT committed.
## Conversation 9
- **Request**: Add diagrams to visualize the ideas in A.5 (attention as kernel regression with a learned metric).
- **Actions**: Built `figures/qkv-kernel-regression.py` (column convention, M symmetric-PD) → new **Figure A.3** in A.5. *Left:* a 2-D feature plane, tokens colored by value response, attention weights as marker size for a query, output `o = Σ a_j v_j = 2.23` vs plain mean −0.12 (the kernel-weighted average of eq 6); weight grows along the `M x_q` direction (alignment, not proximity). *Right:* the attention kernel `exp(x_q^T M x/√d_k)` rises monotonically along the alignment ray while a Gaussian RBF peaks at the query and decays — the RBF caveat made visual. Renumbered the three later figures (sqrt-dk→A.4, low-rank→A.5, induction→A.6) and the one cross-ref; re-wired anchors + link-references.
- **Result**: Gate GREEN (lint 0, all `--check` clean, validate-refs 0/0 with 10 images, citation-sources 60/0); figure labels A.1–A.6 contiguous. NOT committed. (Note: a user-added blue highlight now sits in the A.5 prose — left intact.)
## Conversation 10
- **Request**: "fold these first-principle understandings in a wiki ... making an overwhelming comparison between these methods with the very basic kernel regression" — pointing at an iCloud ChatGPT export (`Kernel-Regression-Explanation.md`) that derives Nadaraya–Watson kernel regression, k-NN, Gaussian processes, RBF networks, and attention from first principles.
- **Actions**: Asked two scoping questions; user chose a new **Appendix B** inside `surveys/llms-for-coding/` and self-contained derivations reusing only existing refs 54/59/60. Wrote `appendix-b-kernel-regression-family.md` (8 sections B.1–B.8, eight numbered equations): the common linear-smoother skeleton + five comparison axes; Nadaraya–Watson from the conditional-expectation density ratio; k-NN, GPs, RBF networks each as a single edit to that form; attention (section B.6) as the headline member with four named departures, each cross-linked to Appendix A (sections A.1–A.9); a 6-row master comparison table + reductions lattice + synthesis. Built two computed matplotlib figures — `kernel-family-fits` (one 40-point dataset → NW/k-NN/GP-with-band/RBF fits, RMSE 0.208/0.192/0.198/0.165) and `kernel-family-weights` (equivalent-kernel weight vectors at one query: NW/k-NN non-negative summing to 1; GP/RBF signed with negative side-lobes, sums 0.996/1.005). Wired `order.json` + `index.md` (item 20). Ran the full toolchain. Ran a 5-lens adversarial-review workflow (math, figure↔data, table, Appendix-A consistency, citation/rendering) and fixed all five nits it surfaced (y_i→y_j index slip in B.6; softened "monotonically" to match A.5's SPD hedge; noted the unnormalized-KDE constant cancels; k-NN support-cell wording; bumped Figure B.2 panel-title sums to 3 dp so 0.996/1.005 show).
- **Result**: Appendix B complete and gate GREEN (lint 0, 8 equations sequential, 12 images valid, 43 cross-file links valid, bare-refs clean, citation-sources 60/0); review found no blocking/major issues. NOT committed — awaiting go-ahead (working tree also still holds the earlier uncommitted Figure A.3 + blue highlight).
- **Findings**: Every family member is the one linear smoother ŷ(x)=Σ wᵢ(x)·yᵢ; basic kernel regression is the non-negative + normalized + local + fixed-isotropic-kernel member, and each relative relaxes exactly one or two of those — the unifying spine of the comparison. GP and RBF equivalent-kernel weights empirically carry small negative side-lobes (min −0.04/−0.05) and do not sum to one (0.996/1.005), the concrete signature of relaxing the convex-average constraint.
<!-- LOG-END -->
