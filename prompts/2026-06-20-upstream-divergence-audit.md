# Conversation Log — 2026-06-20 — session: upstream-divergence-audit

## Conversation 1
- **Request**: "what are the updates compared to the upstream?" — compare this repo (`llm-zero-to-one`) against its upstream `../data-channel-receiver` (the telecom repo the `.claude`/`viewer`/`bench` infra was ported from on 2026-06-13).
- **Actions**: Confirmed upstream = `/Users/claire/GitRepos/data-channel-receiver` via memory note. Ran `diff -rq` over `.claude/`, `viewer/tools/`, `bench/` to map file-level divergence. Launched a background Workflow (`upstream-divergence-audit`, 21 agents) classifying each of the 19 differing shared files (direction: upstream-ahead / here-ahead / divergent / domain-retarget-only) and inventorying the 8 upstream-only infra items as port candidates.
- **Result**: Divergence is ~90% expected telecom→LLM retargeting (docstrings, examples, skill prose — identical logic). Real signal: (1) **2 upstream correctness fixes HERE lacks** — `renumber-paragraphs.py` FOOTNOTE_DEF guard, and the `check-footnote-refs.py` tool + `check-survey.md` step-9 footnote-`note-`-namespace check (HERE's viewer has the identical exposed code paths but no guard/gate, missing bug record 2026-06-15-02); (2) **HERE is ahead** on `survey-explainer-fold` Step-6 sweep (more complete than upstream's, which is stale vs its own toolchain), plus LLM-domain enrichments in phase-3-evidence/preflight/phase-2-outline, plus the in-flight viewer-stack-unification (node_modules/package.json, .viewer-highlights); (3) **upstream-only generic infra never ported** — `.claude/agents/` (evidence-collector/survey-enricher/viewer-dev), `skill-improvement` + `ui-review-loop` skills, commands `add-reference`/`bg`/`enrich`(section-level superset of HERE's enrich-equation)/`refine-plan`/`review-plan`, `log-turn-telemetry.py` hook, `compose-notify-msg.py`+`notify.sh` notifier, `workflow.md` rule. Telecom-specific (skip): method-eval/reference-implementation-study/sim-audit skills, add-3gpp-spec/study-signoff commands, figure-operating-conditions/sim-report-completeness rules, bench/reference-implementation-study.
- **Findings**: The only domain-agnostic `viewer/tools/*.py` with a genuine logic delta is `renumber-paragraphs.py` (footnote guard) — the other four (.py) differ only in docstring/comment example text. `enrich.md`→`enrich-equation.md` was a narrowing, not a rename: the section-level enrichment capability (evidence/structural/cross-ref modes + survey-enricher delegation) was dropped in the port.

## Conversation 2
- **Request**: "only pull to the upstream" — reverse direction: backport HERE's domain-agnostic improvements *into* `../data-channel-receiver`, leaving upstream's telecom content intact.
- **Actions**: Verified upstream is git-initialized + target files clean, and that all five sweep tools (`renumber-equations`, `link-references`, `validate-refs`, `check-citation-sources`, `build-index`) exist in upstream `viewer/tools/` and that upstream's `/check-survey` is slug/`<directory>`-based (its check-survey.md already runs `validate-refs.py <directory>` and `--bare-refs-only --severity=error <directory>`). Applied 5 surgical edits to the upstream working tree: (1) `viewer/tools/init-doc.py` — `npm install` + `-p 4500` serve hint; (2–5) `.claude/skills/survey-explainer-fold/SKILL.md` — expanded Step-6 sweep (added `link-references --check`, cross-file `validate-refs DIR`, `check-citation-sources`, `--severity=error`, canonical `viewer/tools/renumber-equations.py` path, conditional `build-index`), corrected gate wording (removed the false `pre-push`/"cannot be pushed" claim → "delivery gate … does not sign off", added `<survey-slug>`), and matching checklist + cross-ref lines.
- **Result**: Upstream diff = `init-doc.py` (+1 line) and `survey-explainer-fold/SKILL.md` (16 ins / 12 del), telecom worked-instance examples preserved. Left uncommitted (CLAUDE.md: commit only when asked). Deliberately NOT backported: the LLM-domain phase-file enrichments (phase-2 method-taxonomy checklist, phase-3 contamination/version-pinning/model-card-skepticism/search-budget, preflight LLM calibration table) and survey-explainer-fold's citation-source-value pillar + "how large in real models" capability — all LLM-specific, do not fit upstream's telecom mission. Offered the preflight "task-agnostic / narrow-the-question" sizing nugget as an optional extra.
- **Findings**: HERE's expanded sweep wasn't a novel idea — it just mirrors what upstream's *own* `check-survey.md` already enforces (`validate-refs <directory>`, `--severity=error`); upstream's survey-explainer-fold sweep had drifted stale against its own gate, so the backport restores internal consistency rather than adding new policy.

## Conversation 3
- **Request**: "already commit what's there?" — commit the upstream backport.
- **Actions**: Inspected upstream git state before committing. Found the working tree already **clean** for both backported files and the changes already committed in `main`: `e8711a2` (survey-explainer-fold sweep/gate alignment, authored 17:43) and `d656aa1` (init-doc npm/port hint, authored 17:50), both with Conventional-Commit messages + the `Co-Authored-By: Claude Opus 4.8` footer, both ancestors of HEAD, both already on `origin/main` (`origin/main..HEAD` empty). Did NOT run any commit (nothing of mine left uncommitted; the only other dirty items are an unrelated modified `reports/highlight-locatability-2026-05-09.md` + untracked `prompts/*.md`, left untouched).
- **Result**: No action needed — backport already committed and pushed. Reported the state to the user.
- **Findings**: The two backport commits sit *beneath* two newer unrelated deep-research-survey commits (`ce24ee3`, `a1c017c`), and I never issued a commit this turn → a **concurrent Claude session is active in `data-channel-receiver`** and committed my working-tree edits, then advanced `main`. Concurrent sessions in the sibling repo are a live condition to account for when mutating it from here.

<!-- LOG-END -->
