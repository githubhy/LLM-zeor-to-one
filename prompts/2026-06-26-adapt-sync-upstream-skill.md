# Conversation Log — 2026-06-26 — session: adapt-sync-upstream-skill

## Conversation 1
- **Request**: Adapt the "sync upstream" skill from `../pitch-perfector`.
- **Actions**: Read pitch-perfector's `/sync-upstream` command (`.claude/commands/sync-upstream.md`) + its `.claude/upstream-sync.json`; established that this repo (`llm-zero-to-one`) is itself a bootstrap from `../data-channel-receiver` (telecom/3GPP→LLM/AI, per the `claude-infra-ported-from-data-channel-receiver` memory) with no sync mechanism yet. Created `.claude/commands/sync-upstream.md` (remapped the domain table MIR→LLM/AI, the exclude-as-content dirs, the tooling-reality list, and the leakage grep to wireless terms) and `.claude/upstream-sync.json` (upstream `../data-channel-receiver`, `last_synced_commit=7c3a3bf` = last upstream commit ≤ 2026-06-13 bootstrap). Registered `/sync-upstream` in `CLAUDE.md` Commands catalog.
- **Result**: Command + config land green — JSON valid, high-water mark resolves (314 commits in `BASE..HEAD`). Filed decision `2026-06-26-01` (port + baseline choice) and todo `2026-06-26-clean-residual-wireless-leakage-in-viewer-tools`. Next: user can run `/sync-upstream --dry-run` to triage the 314-commit backlog.
- **Findings**: The new command's step-3 leakage grep immediately caught 4 residual wireless example-strings that survived the original 2026-06-13 bootstrap — `viewer/tools/lint-math.py:81` (`ntn-initial-sync-tracking/`), `build-index.py:8,11` (`ntn-survey.md`), `test_lint_math.py:96` (`ldpc-...` comment). Cosmetic (docstring/help/comment only), but proof the gate works; tracked as the todo above. Added a `grep -vE` exclusion for the two by-design provenance files so the gate doesn't self-trip.

## Conversation 2
- **Request**: `/sync-upstream --dry-run`.
- **Actions**: Resolved baseline `7c3a3bf` → upstream HEAD `5d485d7` (314 commits). Diffed config paths; cross-checked all 71 changed files against local presence + byte-identity (key nuance: "Added upstream" ≠ "missing here" — the 2026-06-17 wholesale viewer sync already pulled some in). Classified into re-adapt (~22 locally-present LLM files), reconcile (3), and net-new (~46). Surfaced name/policy mismatches (`enrich.md`→`enrich-equation.md`, `workflow.md` no local counterpart, `pre-push` policy).
- **Result**: Reported deltas, changed nothing. User chose full scope ("everything incl. new skills") and in-session incremental batches (review between).

## Conversation 3
- **Request**: Execute the full port — batch 1 (`deep-research-survey` skill, 14 files).
- **Actions**: Created branch `sync/upstream-2026-06-26`. Dispatched 3 parallel adapter subagents (SKILL+phases / addenda / templates+config), each given the telecom→LLM domain mapping, dead-provenance genericization rule, and a mandatory leakage self-check. Ported: new `scale` axis, `audience/register` axis, `richness` mode + R-* items, agent-hardening gotchas (DRS-HARDEN), R-DEPTH gates, cross-link sign-off; 2 new JSON configs + new `addenda/phase-2.md`. Verified independently: leakage CLEAN, both JSON valid, lint-math 0/0 across 14 files. Fixed a pre-existing `$/1M tokens` lint bug in `phase-2-outline.md`. Genericized two refs (`method-eval` skill, `figure-operating-conditions.md`) that fall outside the delta → todo `port-method-eval-and-figure-conventions`.
- **Result**: Batch 1 complete & green, staged on the branch. Awaiting user review before batch 2 (CLAUDE.md + hooks + viewer/tools).
- **Findings**: "Added upstream in range" must be cross-checked against local presence + byte-identity — 3 files (`check-report-completeness.py`, `gen-root-manifest.js`, `survey-explainer-fold`) already exist locally via the 2026-06-17 sync and need *reconcile*, not *add*. Outbound skill refs must be checked against what the sync actually delivers — 2 of 7 (`method-eval`, `figure-operating-conditions`) were not in the delta and would have dangled.

## Conversation 4
- **Request**: Commit batch 1, then proceed to batch 2 (CLAUDE.md + hooks + viewer/tools + reconcile).
- **Actions**: Committed batch 1 as two self-consistent commits (`300248d` command scaffolding, `8884a83` skill port). Batch 2: per-file delta analysis showed most were no-ops — `init-doc.py`, `check-report-completeness.py`, `gen-root-manifest.js` already == upstream HEAD (via the 2026-06-17 sync). Ported 3 genuinely-behind, domain-agnostic files from HEAD (`status-line.py` git repo/branch segment; `renumber-paragraphs.py` footnote-def skip; `lint-math.py` U+FFFD corruption check), genericizing dead bug-IDs (`2026-06-22-01`, `2026-06-24-01`) + corpus filenames in comments. Cleaned all residual bootstrap leakage in viewer/tools (lint-math/build-index/test_lint_math `ntn-*`/`ldpc` examples → LLM) and closed todo `clean-residual-wireless-leakage-in-viewer-tools`. Deferred `validate-refs-on-dirty.sh` (calls `crosslink.py`) and survey-explainer-fold's `## Cross-link sign-off` section to batch 3; deferred CLAUDE.md catalog reconciliation to the final batch.
- **Result**: Batch 2 green — full-harness leakage CLEAN, py_compile OK, smoke-tested both new behaviors, tests 21/21 + 17/17. Committed. Next: batch 3 (cross-link subsystem).
- **Findings**: Copying an upstream HEAD file wholesale (valid when local==BASE) reintroduces the upstream's *own* wireless docstring examples — always leakage-scrub after a wholesale copy, not just after a hand-splice. Also: "Added upstream" files can already be at HEAD locally (init-doc/check-report-completeness/gen-root-manifest) — verify byte-identity before porting to avoid redundant work.

## Conversation 5
- **Request**: Commit batch 2, proceed to batch 3 (cross-link subsystem).
- **Actions**: Ported the cross-link subsystem (9 files). Mechanical (me): `crosslink.py` (copied HEAD + scrubbed the LDPC docstring/anecdote + dead field-note path + wireless usage example), `test_crosslink.py` (remapped LDPC test fixtures → LLM vocab keeping input↔assertion in sync), `crosslink-severity` (`warn`), `crosslink-scope` (rewritten to `surveys/llms-for-coding` — no `wikis/` here), `validate-refs-on-dirty.sh` (spliced the deferred crosslink-advisory block). Prose remap (subagent): `crosslink.README.md`, `rules/cross-linking.md`, `cross-link/SKILL.md`, + added the `## Cross-link sign-off` section to `survey-explainer-fold` (one section, rest untouched).
- **Result**: Green — full-harness leakage CLEAN, 28 viewer/tools tests pass, hook `bash -n` OK, and `crosslink.py check surveys/llms-for-coding` runs and emits sensible LLM candidates (e.g. `appendix-a-qkv A.5 → appendix-b-kernel-regression B.7`). Committed. Next: batch 4 (new skills + rules + commands).
- **Findings**: Copied TEST files smuggle domain vocabulary in fixtures AND assertions — the doc-oriented leakage net (no `min-sum`/`density-evolution`/`sign-bit`) missed `test_crosslink.py` entirely on first pass. Scan tests with an expanded term set and remap input+assertion together. Carry-forward: `cross-linking.md` references `.githooks/pre-push`, which lands with its policy decision in the final batch.

## Conversation 6
- **Request**: Proceed to batch 4 (new skills + rules + commands).
- **Actions**: Ported the net-new harness components via 4 parallel adapter subagents (disjoint subtrees, each given the telecom→LLM mapping + dead-provenance genericization + leakage self-check): **capability-decomposition** (16 files — upstream was already domain-neutral, only `meta.example.json` got a concrete LLM remap to a decoding/sampling library example), **ui-review-loop** (11 files — Playwright viewer-review scripts, domain-agnostic; SKILL + default `--file` retargeted to `appendix-a-qkv-first-principles.md`), **reference-implementation-study** (full **16-file** tree — heaviest sim→LLM remap: signal model→task/data distribution, BLER-vs-SNR waterfall→quality-vs-budget curve, finite-precision→quantization), and a **bundle** (`sim-audit/SKILL.md`, rules `deferred-tracking.md`/`sim-report-completeness.md`/`workflow.md`, command `study-signoff.md`, tool `check-footnote-refs.py`). Verified globally: full-harness leakage CLEAN, py_compile/json.tool/node --check all pass, `test_leafkey.py` 3/3, `check-footnote-refs.py` runs end-to-end on the real corpus (exit 0). Fixed 3 dangling refs to un-ported siblings (`method-eval` ×2 in sim-audit, `figure-operating-conditions.md` in sim-report-completeness) by genericizing — same treatment as batch 1; broadened todo `port-method-eval-and-figure-conventions` to track all three referencing components. Confirmed `enrich.md` delta is a **no-op** (local `enrich-equation.md` already points at `.claude/rules/` for math rules). Filed decision `2026-06-26-02` (full-RIS-tree port rationale).
- **Result**: Batch 4 green — 6 net-new components (≈44 files) landed on the branch, all gates pass, no dangles. Committed. Next: final batch 5 — CLAUDE.md catalog reconciliation (register the new skills/rules/command + Agent-Fan-Out / Todo / Validation-Hooks deltas), `.githooks/pre-push` policy call, advance the sync mark to `5d485d7`, final `/check-survey`.
- **Findings**: The `BASE..HEAD` diff undercounts a skill that *predates* the mark and was never bootstrapped — `reference-implementation-study` shows 4 changed files but is a 16-file skill absent locally, so the delta is unapplicable without porting the full tree first. Lesson: for an absent skill, cross-check the diff window against the full upstream tree (`git ls-tree`), not just `diff --name-status`. Also: upstream subtrees can already be domain-neutral (capability-decomposition teaches with a generic HTTP-client example) — verify before assuming a wireless remap is needed, to avoid gratuitous edits that risk code-fidelity.
## Conversation 7
- **Request**: Final batch 5 — CLAUDE.md catalog reconciliation + `.githooks/pre-push` (user chose **"Port and activate"**) + advance the mark.
- **Actions**: **CLAUDE.md** (6 surgical edits, re-adapted onto the local file, dead provenance genericized): inserted the two **Agent Fan-Out** sections (Model Selection; Sizing/Failure Diagnosis — dropped the upstream's dead `bugs/2026-06-25-02` + `field-notes/...channel-models...` refs, kept the transferable step-cap lesson); strengthened **Todo Capture** (every downstream/handoff item gets a todo); updated **Validation Hooks** (removed the stale "not git-initialized" line; added the crosslink advisory to the Stop-hook row; new **Cross-link gate toggle + scope** paragraph; rewrote the pre-push paragraph from "optional/not installed" to **active**); registered the 3 new **rules** (cross-linking, sim-report-completeness, deferred-tracking — NOT figure-operating-conditions, un-ported), the 5 new **skills** (cross-link, capability-decomposition, reference-implementation-study, sim-audit, ui-review-loop), and the `/study-signoff` **command**. Ported `.githooks/pre-push` (adapted `python`→`python3`; **rewrote the validator loop to iterate per `surveys/*/order.json`** because flat `surveys/` makes renumber-equations/paragraphs exit 1 and blocked every push), `chmod +x`, activated via `git config core.hooksPath .githooks`. Advanced `.claude/upstream-sync.json` mark `7c3a3bf`→`5d485d7f9222…` + date 2026-06-27 + corrected note. Filed decision `2026-06-27-01`.
- **Result**: **Sync complete.** Pre-push gate **runs green** on the live corpus (both surveys clean; 5 advisory cross-link gaps listed, non-blocking). Final full-harness leakage sweep **CLEAN** (only by-design self-hits: the `/sync-upstream` description line + mapping table); 28 viewer/tools tests pass; all JSON valid. Committed batch 5. The 314-commit upstream backlog is now fully synced in 5 reviewable batches.
- **Findings**: A ported hook can be byte-clean of domain leakage yet still **wrong for the local layout** — the upstream's flat-`surveys/` invocation silently inverts to "block every push" here because the renumber tools resolve a survey from its own `order.json` dir. Validate a ported hook by *running* it, not just by reading it. Also: `core.hooksPath` activation keeps the hook version-controlled (no `.git/hooks/` copy), but is local config a fresh clone must re-set — documented in CLAUDE.md.
## Conversation 8
- **Request**: After the sync merged+pushed, inspect the 5 remaining untracked files and commit the ones that should not be local-only.
- **Actions**: Triaged each. (1) `prompts/2026-06-20-{upstream-divergence-audit,viewer-stack-unification}.md` — legit conversation logs for two real prior sessions; `prompts/` is version-controlled (`.gitkeep` + 3 tracked logs) and these were simply never committed → **committed**. (2) `surveys/llms-for-coding/.viewer-highlights/appendix-a-qkv-first-principles.md.json` — viewer-generated per-user reader state (empty `"highlights": []`, keyed to a doc revision) → **gitignored** (`.viewer-highlights/`), not committed. (3) root `package.json`+`package-lock.json` (declare only `ws`) — `ws` is used solely by `viewer/serve.js`, and `viewer/package.json` already declares it; never tracked; residue of the incomplete 2026-06-20 "viewer-stack-unification" effort → **left untracked, flagged to user** (not landing a half-done refactor that duplicates the viewer manifest).
- **Result**: Committed the 2 session logs + the `.gitignore` rule; pushed to `origin/main`. Working tree now shows only the 2 root `package*.json` files (intentionally left for the unification effort / user's call). Pre-push gate green on push.
- **Findings**: The untracked set was three different categories masquerading as one "noise" bucket — tracked-artifact-type-not-yet-committed (logs), genuinely-local machine state (highlights → ignore), and incomplete-refactor residue (root npm files → neither commit nor ignore, surface to owner). "Untracked" alone doesn't tell you which; the file's *role* does.
<!-- LOG-END -->
