# Conversation Log — 2026-06-28 — session: multimodal-llms-survey

## Conversation 1
- **Request**: `/deep-research-survey multi-modal LLMs`, then re-invoked as `proposed, scale:wide on multi-modal LLMs`.
- **Actions**: Ran the deep-research-survey skill in `proposed` mode (all P+R items), `scale: wide`, `audience: learner`. **Phase 1 (Scope):** read phase-1 + the P0-3 brief-gate addendum + the wide-scale config; wrote the research brief to `surveys/multimodal-llms/_scratch/brief.md`; surfaced a single P0-3 confirmation via AskUserQuestion. User chose **full omni-modal parity** (vision+audio+video+any-to-any all at full derivation depth) and **learner** register (first-principles prerequisites + SP analogies). Locked both into the brief. **Phase 2 (Outline):** read phase-2 + the R-GOV/R-CARD/R-SURVEY addendum; wrote the full outline to `_scratch/outline.md` (depth tiers H/L/C per R-GOV with learner-fundamentals floor; R-CARD 10-element method cards; R-SURVEY artifacts — comparison matrix, quantitative SOTA, notation contract, open-problems handoff, Q&A appendix; per-section research questions + MH/NH flags; MAST coverage-gap check). Scaffolded the 23-file skeleton (order.json + index.md + references.md with source-tag header + 21 lint-safe stubs); verified manifest-consistent + lint-clean. **Phase 3 (Evidence) start:** read phase-3 + the DRS-HARDEN/P0-1/P1-1 addendum; smoke-tested the source-fetch resolver (CLIP OK, 6.8MB); launched a background batch fetching ~30 foundational+core papers by arXiv id into `download/` (`_scratch/fetch-batch.sh`).
- **Result**: Phases 1–2 complete; Phase 3 acquisition running in background. Tasks #9 (done), #10 (in-progress, acquisition), #11/#12 pending. Brief + outline persisted as the north star.
- **Findings**: download/ had the core/code-LLM corpus but NO multimodal papers — full acquisition needed. Wide scale (searches≤40) is above the measured death boundary (~28), so evidence agents will run hardened (file-first _scratch, exact paths/no-Glob, WebFetch≤2, ~3-4 Q/agent, empty-return-as-death, retry≤2). Reused the llms-for-coding multi-file layout as the structural model; fundamentals will cross-link to its QKV/attention appendices rather than re-deriving the base transformer.
## Conversation 2
- **Request**: (continuation) — acquisition completed; proceed with evidence + start synthesis.
- **Actions**: Verified all **30 acquired PDFs** title-match their intended paper (Step-4 content-confirm via pymupdf; 0 mismatches). Launched the **evidence Workflow** (`_scratch/evidence.workflow.js`, run `wf_5f49edf4-8b9`): 7 hardened Sonnet `evidence-collector` clusters (E1 eval-landscape, E2 open SOTA numbers, E3 closed-frontier+deployment-gap, E4 inference/compression, E5 audio/video/omni, E6 training/alignment, E7 catalog/connector breadth), file-first `_scratch/evidence-E*.md` ledgers, exact local paths/no-Glob, WebFetch≤2, empty-return-as-death + trimmed retry. Confirmed file-first working (ledgers writing incrementally). Began Phase 4: wrote the R-SURVEY **notation contract** into `index.md` (the externalized symbol memory for memory-guided synthesis), lint-clean.
- **Result**: 30 sources verified in download/; evidence Workflow running in background (will notify); notation contract seeded. Phase 4 synthesis to resume on the main thread (depth from local PDFs) once ledgers land, written sequentially in dependency order.
- **Findings**: The ported `evidence-collector` agent has tools {Read,Grep,Glob,WebSearch,WebFetch,Bash} but **no Write** — DRS-HARDEN file-first is satisfied via Bash heredoc append (instructed in the cluster brief). Worth noting for a future agent-toolset tweak. Division of labor held: Sonnet agents do web-gap breadth (numbers/landscape/catalog), main thread (Opus) owns the derivational depth from local primaries.
## Conversation 3
- **Request**: Evidence collector for Cluster E6 — training data & multimodal alignment (3 questions: LLaVA recipe, pretraining datasets, hallucination mitigation).
- **Actions**: Read liu-llava-2023.pdf (pages 0-7) and liu-llava-1.5-2023.pdf (pages 0-10) for Q1. Ran 7 web searches for Q2/Q3. Wrote 11 ledger entries to surveys/multimodal-llms/_scratch/evidence-E6.md.
- **Result**: Ledger complete with 11 entries across Q1 (3), Q2 (4), Q3 (4). All primary sources found (A/B tier). COYO-700M source tag is B/med due to no confirmed primary arXiv from searches.
## Conversation 4
- **Request**: (continuation, main thread) — evidence Workflow completed; verify ledgers + advance Phase 4.
- **Actions**: Evidence Workflow `wf_5f49edf4-8b9` finished: 6 clusters alive first-pass, **E2 recovered on the trimmed retry**, none still dead (8 agents, 494K tok, 147 tool calls, 7 min) — the wide-mode systematic-death-then-recover pattern the retry policy targets. MAST attribution pass: ledgers are gold-grade (E1 has page-level local citations + tier/confidence; E3 correctly self-flagged every closed-model number UNVERIFIED/tier-B/not-load-bearing). **Phase 4:** added the 14-entry foundational reference set to `references.md` (metadata read from the verified PDFs, not memory; 12 `local:` strong + 2 `web` weak); wrote the first full section **§1 introduction-and-scope.md** at learner register (the three design axes — entry-point/fusion/generation — with SP analogies, cross-survey plain-links to the llms-for-coding transformer fundamentals). Ran the marker sweep: renumber-sections clean, renumber-paragraphs --init (11 anchors), validate-refs survey-wide 0/0, check-citation-sources 0 errors, lint-math clean.
- **Result**: Phase 3 complete (task #10 done). Phase 4 underway (#11): notation contract + §1 done and fully validated end-to-end. Remaining: §2–§14 + appendices A–F + Q at learner R-DEPTH — the bulk, spanning future sessions; all persisted + resumable from `order.json` + `_scratch/`.
- **Findings**: link-references.py is single-file (wants cites + a `## References` in one doc); the split multi-file layout relies on validate-refs.py for cross-file cite↔bib validation, and cites must be authored in final linked form (`<!-- cite:N -->[[N]](#ref-N)`). Section/para markers authored inline pass renumber --check with no rewrite.
## Conversation 5
- **Request**: (continuation) "continue" — resume Phase 4 synthesis, write §2 Fundamentals.
- **Actions**: Read the precise source equations (ViT Eq 1–4 from dosovitskiy-vit-2020.pdf pp.3–4; CLIP symmetric-InfoNCE pseudocode + temperature clip from radford-clip-2021.pdf pp.4–6; SigLIP softmax/sigmoid losses + bias-init from zhai-siglip-2023.pdf pp.1–3). Added SigLIP as reference [15] (arXiv:2303.15343, local) — arXiv id confirmed from `_scratch/fetch-batch.sh`, not memory. Wrote **§2 fundamentals.md** at headline depth, learner register: §2.1 the discrete-machine-vs-continuous-field obstacle (SP front-end analogy: partition→summarize→align), §2.2 patch embedding (ViT Eq 1, worked 224²/14²=256-patch example, learned-filterbank intuition, N_v=HW/P² → 4th-power resolution cost, Fuyu encoder-free alt), §2.3 the encoder (ViT Eq 2 pre-norm block, attention cross-linked to llms-for-coding appendix, bidirectional vs causal, inductive-bias/data-hunger), §2.4 CLIP contrastive alignment (scaled-similarity Eq 3, symmetric-InfoNCE Eq 4, worked temperature-sharpening example τ=0.07 vs 1, matched-filter analogy, 76.2% zero-shot, single-vector bottleneck), §2.5 SigLIP sigmoid (Eq 5, per-cell binary reframing, bias-init for the N²−N imbalance, worked init example, <16k-batch regime), §2.6 the connector (Eq 6 g_φ projection + early-fusion concatenation, LLaVA single-linear floor → MLP, token-count-as-design-axis handoff to §3/appendix-C). 6 numbered equations.
- **Actions (sweep)**: renumber-equations (6 tags sequential), renumber-sections, renumber-paragraphs --init (37 anchors); converted one untagged worked-arithmetic display block to inline to clear a lint warning, re-ran renumber-paragraphs.
- **Result**: §2 fully validated — lint-math 0/0, validate-refs survey-wide 0/0 (6 eq markers/anchors, 4 cross-file links valid), check-citation-sources 15 entries / 13 local / 2 web / 0 errors, all eq/sec/para --check clean. Phase 4: §1 + §2 done. Next: §3 architecture-building-blocks (connector taxonomy + fusion mechanisms).
- **Findings**: lint-math flags *every* untagged standalone `$$` as a warning (not just results); idiomatic fix for intermediate worked arithmetic is inline math or an `aligned` block inside a tagged eq, not a bare display block. The per-file equation numbering restarts at (1) per section file — cross-file eq disambiguation is by file context, matching the llms-for-coding layout.
## Conversation 6
- **Request**: (same turn, continued) — write §3 Architecture building blocks.
- **Actions**: Read the §3 primaries (Flamingo Perceiver Resampler + GATED XATTN-DENSE pseudocode + interleaved-likelihood Eq 1 from alayrac-flamingo-2022.pdf pp.4–7; BLIP-2 Q-Former two-stage design, 32×768 bottleneck, soft-prompt FC, catastrophic-forgetting framing from li-blip2-2023.pdf pp.3–5). Wrote **§3 architecture-building-blocks.md** at headline depth: §3.1 the encoder→connector→LLM template + 4 knobs, §3.2 entry-point axis (encoder-based vs encoder-free/Fuyu), §3.3 the connector taxonomy (token-preserving MLP projector vs token-reducing Q-Former/Perceiver-Resampler/pooling; Perceiver-Resampler Eq 1, Q-Former soft-prompt Eq 2; worked token-bill example 576 vs 32 vs 64 at 336px; AnyRes/native-res practice), §3.4 fusion axis (early concatenation / deep gated-cross-attention Eq 3 with the zero-init tanh-gate worked example / native-token Chameleon; Flamingo interleaved-likelihood Eq 4), §3.5 frozen-vs-trained + the soft-prompt unifying view. 4 numbered equations.
- **Result**: §3 fully validated — lint 0/0, validate-refs survey-wide 0/0 (10 eq markers/anchors across 2 files, 8 cross-file links valid incl. secxref to §2.2/§2.4/§2.6). Phase 4: §1 + §2 + §3 done (the conceptual core: obstacle → perception stack → architecture design space). Next: §4 method-inventory (R-CARD uniform method cards).
- **Findings**: secxref cross-file links (architecture→fundamentals) resolve cleanly via order.json now that the target file has sec-anchors — confirms the multi-file cross-link toolchain works section-to-section, not just intra-file. The zero-init tanh-gate (Flamingo) is the cleanest worked example in the survey so far of "begin as a known-good identity, deform continuously" — a reusable pedagogical hook for the learner register.
## Conversation 7
- **Request**: "commit" — checkpoint the survey foundation (§1–§3 + evidence + sources).
- **Actions**: Consolidated session logs — two evidence-sub-agents (clusters E5/E1 and E3) had autonomously spawned their own fragment session logs (`multimodal-llm-evidence.md`, `multimodal-llm-survey.md`); their substance is already preserved in the evidence ledgers and Conversations 2/4 here, so removed both (untracked, never committed) to restore one-log-per-session. Committed the foundation to `main` (repo convention is single-author direct-to-main): order.json + index.md (notation contract) + references.md (15 entries) + §1/§2/§3 + 7 evidence ledgers + `_scratch/` brief/outline/workflow + 30 source PDFs in `download/` (kept tracked to satisfy the references.md↔download/ citation-integrity invariant, matching the 55 PDFs already tracked).
- **Result**: Foundation checkpointed. Phase 4 continues at §4 method-inventory next.
## Conversation 8
- **Request**: (continuation after commit) — write §4 method-inventory.
- **Numbering reconciliation**: outline anticipated architecture as §3 (connectors) + §4 (fusion) → method-inventory=§5. I wrote architecture as one cohesive §3 (fusion = §3.4), so the clean one-section-per-file scheme makes **method-inventory = §4** and shifts all downstream files down by one (training=§5, generation=§6, modality=§7, inference=§8, eval=§9, comparison=§10, SOTA=§11, design=§12, open-problems=§13). order.json (no section numbers) and index.md (defers to order.json) need no change; the _scratch/outline.md §-numbers are now stale but it's a planning scratch doc. Mechanical renumber, no decision file.
- **Actions**: Harvested evidence ledgers E7 (catalog/connector breadth) + E2 (open SOTA numbers) + E5 (audio/video/omni) — all tier-A, primary-traced — for card metadata; read the generation primaries I hadn't (VQ-VAE codebook+straight-through+3-term loss from vandenoord pp.3–4; Transfusion one-transformer LM+DDPM joint loss + intra-image bidirectional attention from zhou pp.3–5). Pulled the authoritative arXiv-id↔file↔title map from fetch-batch.sh (no memory citations). Added **23 references [16]–[38]** (17 strong local, 4 web, 4 abstract-only — weak tags used only for non-load-bearing catalog entries). Wrote **§4 method-inventory.md** as the uniform R-CARD register: a fixed 6-field card (What&where / Mechanism / Intuition / Numbers / Limits / Status+epistemic-tag) mapping the 10 R-CARD elements, derivations cross-referenced not repeated. H cards §4.1–4.6 (CLIP, ViT, LLaVA, BLIP-2, Flamingo) + §4.11–4.13 (VQ-VAE/VQGAN, Chameleon, Transfusion) with 3 new equations (VQ quantization Eq 1, VQ 3-term loss Eq 2, Transfusion combined loss Eq 3); L cards §4.3/4.7–4.10/4.14–4.16 (SigLIP, Qwen2/2.5-VL, InternVL, Fuyu, LLaVA-NeXT, Emu3, Whisper, Qwen-Audio/SALMONN); C-catalog table §4.17 (15 systems incl. closed frontier, each with epistemic tag + source tag).
- **Actions (sweep)**: renumber-equations (3 tags), renumber-paragraphs --init (22 anchors); fixed 5 cross-file refs that I'd written as same-file `secref:3.x` → `secxref:3.x` with architecture-building-blocks.md path (renumber-sections orphan check caught the broken links).
- **Result**: §4 fully validated — lint 0/0, validate-refs survey-wide 0/0 (13 eq markers across 3 files, **18 cross-file links valid**), check-citation-sources 38 entries / 30 local / 8 weak / 0 errors, renumber --check clean. Phase 4: §1–§4 done (foundation + complete method inventory). Next: §5 training-and-alignment.
- **Findings**: `secref` (same-file) vs `secxref` (cross-file) is caught by renumber-sections' in-file orphan check, NOT by validate-refs (which only checks link syntax validity) — so a cross-file ref mistakenly written as `secref` produces a *syntactically valid but broken* link that validate-refs passes; renumber-sections is the gate that catches it. Spaced prose "§ 2.4" mentions are not matched by the bare-ref detector (it keys on the no-space marker form), so casual back-pointers in prose are allowed while load-bearing refs use markers.
## Conversation 9
- **Request**: (continuation after committing §4) — write §5 training-and-alignment.
- **Acquisitions**: To ground §5's data + alignment landscape in primaries (not the E6 ledger alone), acquired 5 papers via source-fetch — LAION-5B (2210.08402), MMC4 (2304.06939), ShareGPT4V (2311.12793), RLHF-V (2312.00849), mDPO (2406.11839); all title-confirmed via pymupdf. Read RLHF-V pp.1–4 (DDPO segment-weighted preference) + mDPO pp.1–4 (conditional + anchored objectives, the unconditional-preference pathology) + the 3 data-paper abstracts (LAION 5.85B/2.32B-EN CLIP-filtered; MMC4 101.2M docs/571M imgs/43B tok; ShareGPT4V 1.2M from 100K GPT-4V seeds). Added references [39]–[43] (all strong local).
- **Actions**: Wrote **§5 training-and-alignment.md**: §5.1 pretraining objectives (contrastive xref §2.4 + generative/interleaved xref §3.4) + the data-quality lever (LAION→MMC4→ShareGPT4V); §5.2 the LLaVA recipe at headline depth (two-stage freeze schedule with the 2e-3/2e-5 LR asymmetry, GPT-4 symbolic-proxy instruction-data manufacture, the answer-tokens-only objective Eq 1, worked ironing-taxi training step); §5.3 preference alignment at load-bearing depth (multimodal DPO Eq 2 → RLHF-V Dense-DPO segment weighting Eq 3 → mDPO conditional+anchored Eq 4, fixing the image-ignoring unconditional-preference failure); §5.4 the frozen-vs-trained schedule ("touch the most-pretrained component least"). 4 numbered equations.
- **Actions (sweep)**: renumber-equations (4 tags), renumber-sections (+ --check clean), renumber-paragraphs --init (19 anchors); all cross-file refs authored as secxref from the start (no secref/secxref slip this time).
- **Result**: §5 fully validated — lint 0/0, validate-refs survey-wide 0/0 (17 eq markers across 4 files, 24 cross-file links valid), check-citation-sources 43 entries / 35 local / 8 weak / 0 errors. Phase 4: §1–§5 done. Next: §6 multimodal-generation (the discrete-AR vs continuous-diffusion tension; needs Chameleon/Janus depth + DDPM — VQ-VAE & Transfusion already read).
- **Findings**: source-fetch loop of 5 papers exceeded the 2-min Bash default (LAION is 17 MB); split into two calls with an explicit longer timeout. The acquisition-before-citation discipline paid off: LAION's abstract independently re-confirmed CLIP's 11.5%→76.2% ImageNet zero-shot number used in §2/§4 — a free cross-check that the value isn't a memory artifact.
## Conversation 10
- **Request**: (continuation after committing §5) — write §6 multimodal-generation.
- **Acquisitions/reads**: Read Chameleon pp.2–4 (image tokenizer: 512² → 1024 tokens, codebook 8192, unified BPE vocab 65,536, ~10T tokens, begin/end-image markers) + Janus pp.2–5 (decoupled SigLIP-understanding vs VQ-generation encoders, one AR transformer, three-stage training, AR cross-entropy). Acquired DDPM (Ho et al. 2020, arXiv:2006.11239) in the background and read pp.3–4 (forward reparameterization, ε-prediction Algorithm 1/2, the simplified L objective) to ground the diffusion equations from the origin, not just Transfusion's restatement. Added reference [44] DDPM (strong local).
- **Actions**: Wrote **§6 multimodal-generation.md** — generation as "perception in reverse," organized around the discrete-AR vs continuous-diffusion design question: §6.1 discrete-AR route at headline depth (AR factorization over VQ codes Eq 1, Chameleon/Emu3, worked 1024-token-image figure, OCR/lossy limits; VQ derivation xref §4.11/appendix-D not repeated); §6.2 continuous-diffusion route at headline depth (DDPM forward closed form Eq 2 with the SNR-sweep SP reading, the ε-prediction objective Eq 3 + why predict noise, Transfusion one-transformer-two-objectives xref §4.13); §6.3 Janus decoupling at load-bearing depth (understanding wants semantic abstraction, generation wants spatial detail; one encoder is a compromise); §6.4 the central tension (fidelity/speed/unification/tooling axes, no settled winner — the live open question). 3 numbered equations.
- **Actions (sweep)**: renumber-equations (3 tags), renumber-sections (+ --check clean), renumber-paragraphs --init (15 anchors); all cross-file refs secxref from the start.
- **Result**: §6 fully validated — lint 0/0, validate-refs survey-wide 0/0 (20 eq markers across 5 files, 30 cross-file links valid), check-citation-sources 44 entries / 0 errors. Phase 4: §1–§6 done (foundation + build + train + generate — the complete forward and reverse pipeline). Next: §7 modality-breadth (video/audio/omni; E5 ledger + Whisper card already in hand).
- **Findings**: grounding diffusion from the DDPM primary (not Transfusion's secondhand restatement) let §6.2 explain *why* ε-prediction is the stable parameterization (stationary unit-variance target) — a depth the secondary source wouldn't have supported. The background-fetch-while-reading pattern (DDPM fetch ran while I read Chameleon+Janus) hid the acquisition latency entirely.
## Conversation 11
- **Request**: (autonomous run) — write §7 modality-breadth.
- **Actions**: Wrote **§7 modality-breadth.md** from the E5 ledger + §4 audio cards (no new reads): §7.1 audio at headline depth — the log-mel spectrogram front-end derived as STFT + mel filterbank + log (Eq 1), framed as "patch embedding for sound" (the SP-native bridge: STFT tiles time, mel pools frequency, vision learns its filterbank while audio inherits a designed one); Whisper front-end, Qwen-Audio early-fusion vs SALMONN window-Q-Former, AudioPaLM discrete-audio-token native fusion + text-LLM translation transfer. §7.2 video at load-bearing depth — sparse uniform sampling (Video-LLaVA 8-frame), the token-budget wall (576 tokens/frame), temporal compression + M-RoPE time encoding, long-video as a sampling-theory-for-token-budget problem. §7.3 omni/real-time — GPT-4o end-to-end any-to-any (contested source), Moshi full-duplex multi-stream + inner-monologue; encode-then-respond → interleave-and-stream shift. 1 equation.
- **Actions (sweep)**: renumber-equations/sections/paragraphs --init; fixed one secref:6.1 → secxref (multimodal-generation.md).
- **Result**: §7 validated — lint 0/0, validate-refs survey-wide 0/0 (21 eq markers across 6 files, 37 cross-file links). Phase 4: §1–§7 done. Next: §8 inference-and-serving (E4 ledger).
## Conversation 12
- **Request**: (autonomous run) — write §8 inference-and-serving.
- **Acquisitions/reads**: From E4 ledger; acquired + read FastV (chen-fastv-2024, ECCV 2024) and ToMe (bolya-tome-2023, ICLR 2023) abstracts, plus PruMerge (shang-prumerge-2024) local. Added references [45]–[47] (all strong local).
- **Actions**: Wrote **§8 inference-and-serving.md**: §8.1 the vision-token cost problem at headline depth (prefill quadratic cost model Eq 1 with the AnyRes token-count formula; worked 1344² ≈ 2304-token / LLaVA-NeXT 2880-token / Qwen2-VL 16,384-token figures; quality-lever-is-cost-lever framing; prefill dominance); §8.2 compression taxonomy at load-bearing depth (architectural resamplers recap, ToMe encoder-side bipartite merging, FastV LLM-side pruning via the 0.21%-deep-layer-attention insight + the FlashAttention-incompatibility caveat, PruMerge select+merge); §8.3 KV-cache (vision tokens as heterogeneous cache citizens, streaming-video unbounded growth). 1 equation.
- **Result**: §8 validated — lint 0/0, validate-refs survey-wide 0/0 (42 cross-file links), check-citation-sources 47 entries / 0 errors. Phase 4: §1–§8 done. Next: §9 evaluation-and-benchmarks (E1 ledger, gold-grade).
## Conversation 13
- **Request**: (autonomous run) — write §9 evaluation-and-benchmarks.
- **Acquisitions/reads**: From E1 ledger (gold-grade, MMMU+POPE local); acquired + read MMStar (chen-mmstar-2024, NeurIPS 2024) pp.1–2 to ground the vision-indispensability/leakage critique. Added reference [48].
- **Actions**: Wrote **§9 evaluation-and-benchmarks.md** at load-bearing depth: §9.1 benchmark landscape by capability (general reasoning MMMU/MMBench/MME; doc-OCR DocVQA/TextVQA/ChartQA/AI2D; math MathVista; metrics ANLS/relaxed-acc/CircularEval; MMMU anchored deep-dive — human 88.6% vs ~69% models; the rate-statistic CI rigor note); §9.2 hallucination (POPE balanced-binary object probing, random/popular/adversarial monotonic drop, F1 primary, loop to §5.3 alignment); §9.3 the validity crisis — the multimodal-gain diagnostic (Eq 1), GeminiPro 42.9% on MMMU with no image, Sphinx +17.9% leakage over its LLM backbone, MMStar's vision-indispensable fix + MG/ML metrics; prompt sensitivity, LLM-judge bias, contamination. 1 equation.
- **Result**: §9 validated — lint 0/0, validate-refs survey-wide 0/0 (43 cross-file links), check-citation-sources 48 entries / 0 errors. Phase 4: §1–§9 done. Next: §10 comparison-and-tradeoffs (R-SURVEY master matrix, pure synthesis from §4 cards).
## Conversation 14
- **Request**: (autonomous run) — write §10 comparison-and-tradeoffs.
- **Actions**: Wrote **§10 comparison-and-tradeoffs.md** at headline-synthesis depth (pure synthesis from §4 cards, no new sources): §10.1 the master comparison matrix — 11 full-system rows (Flamingo/BLIP-2/LLaVA-1.5/LLaVA-NeXT/Qwen2-VL/InternVL/Fuyu/Chameleon/Emu3/Transfusion/Janus) × 8 columns (entry-point/connector/fusion/resolution/generates/trained-vs-frozen/defining-cost), with a top-to-bottom reading of the field's tradeoff structure; §10.2 the decision table — 8 deployment targets → recommended design → justifying section, plus the two cross-cutting rules of thumb (resolution-bought-in-tokens; freezing-follows-data-budget). 0 equations (tables + prose). Tables kept math-free to avoid pipe conflicts; clickable secxrefs in prose, spaced prose §-pointers in table cells.
- **Result**: §10 validated — lint 0/0, validate-refs survey-wide 0/0 (52 cross-file links valid). Phase 4: §1–§10 done. Next: §11 state-of-the-art-and-practice (E2/E3 ledgers).
## Conversation 15
- **Request**: (autonomous run) — write §11 state-of-the-art-and-practice.
- **Acquisitions/reads**: From E2/E3 ledgers; E3 correctly flags all closed-model numbers UNVERIFIED tier-B/C. To make the SOTA table load-bearing, **personally re-read** the Qwen2.5-VL report Table 3 (bai-qwen2.5-vl pp.10–12) — verified MMMU(val)/MathVista(mini)/MMStar for Qwen2.5-VL family, Qwen2-VL-72B, InternVL2.5-78B, GPT-4o-0513, Claude-3.5-Sonnet directly. The closed-model numbers there are third-party-compiled (peer comparison), stronger than E3's secondary-web ones.
- **Actions**: Wrote **§11 state-of-the-art-and-practice.md** at headline depth: §11.1 quantitative SOTA snapshot (verified table cited to [26], with the explicit third-party-compilation + §9-caveat discipline; the load-bearing finding = open frontier has caught closed: Qwen2.5-VL-72B MMMU 70.2 ≥ GPT-4o 69.1, leads MathVista 74.8 vs 63.8; human ceiling 88.6% still far above all); §11.2 deployment-gap thesis (parity removes the quality excuse + cost/data-sovereignty + fine-tunability → open early-fusion dominates self-hosted use; Molmo open-data near-parity = gap is investment not intrinsic); §11.3 stage-by-stage dominant-practice map + the one-sentence consensus (frozen SigLIP/CLIP ViT @ native res → MLP projector → early fusion → instruction+preference-aligned open LLM → token-pruned serving; generation the lone unsettled stage). 0 equations.
- **Result**: §11 validated — lint 0/0, validate-refs survey-wide 0/0 (65 cross-file links). Phase 4: §1–§11 done. Next: §12 design-guidance, §13 open-problems (+ file the reference-implementation handoff todo).
- **Findings**: re-reading the primary SOTA table (vs trusting the tier-A ledger) upgraded the closed-model numbers from UNVERIFIED-secondary-web to third-party-peer-compiled-and-read — the right call for a load-bearing SOTA table, and exactly what citation-integrity's "reproduce every number from source" intends.
## Conversation 16
- **Request**: (autonomous run) — write §12 design-guidance + §13 open-problems-and-roadmap.
- **Actions**: Wrote **§12 design-guidance.md** (L, synthesis): §12.1 a 6-step decision order (modalities/direction → detail-threshold/resolution/connector → many-vs-one-image → data-budget/freezing → trustworthiness/alignment → serving/compression), each step constraining the next; §12.2 a worked walkthrough (on-prem document-QA assistant) that re-derives the §11.3 consensus design from the requirement. Wrote **§13 open-problems-and-roadmap.md** (L, synthesis): §13.1 six structured gaps (unified-generation-at-parity, vision-token-efficiency-wall, hallucination-not-solved, evaluation-validity, fine-grained-grounding, open-source-real-time-omni) each as known/unknown/why/state-of-attack tracing to a prior section's open tension; §13.2 the reference-implementation handoff nominating 2 study-ready methods (FastV-style token pruning vs LLaVA-1.5 baseline, ~45% FLOP @ <1pt; connector ablation MLP vs Q-Former). 0 equations.
- **Todo filed**: `todos/2026-06-28-multimodal-llms-reference-impl-handoff.md` (+ INDEX) — the §13.2 reference-implementation-study candidates, per Todo Capture (a survey roadmap/handoff must be tracked even unprompted).
- **Result**: §12+§13 validated — lint 0/0 each, validate-refs survey-wide 0/0 (92 cross-file links). **Phase 4 body sections §1–§13 COMPLETE.** Remaining: §0 executive-summary, appendices A–F + Q, then Phase 5 gates (citation-audit, /check-survey, cross-link, report).
## Conversation 17
- **Request**: (autonomous run) — write §0 executive-summary.
- **Actions**: Wrote **§0 executive-summary.md** (R-SURVEY Section 0, written last now the body is done): the 60-second verdict (the one-problem/three-axes framing, the converged open-practice default, the load-bearing finding that open has caught closed on understanding benchmarks, generation the lone unsettled stage); a who-should-read-what guide; and a 14-row claims→evidence spine table mapping every load-bearing claim to its establishing section. sec-0 numbering; 0 equations.
- **Result**: §0 validated — lint 0/0, validate-refs survey-wide 0/0 (111 cross-file links). Body + exec summary complete. Next: appendices A–F + Q (fetching CPC for appendix-B's InfoNCE-as-MI-bound).
## Conversation 18
- **Request**: (autonomous run) — write appendices A and B.
- **Actions**: Wrote **appendix-a-vit-and-encoders.md** (the encoder internals §2.3 deferred): A.1 multi-head self-attention encoder version (Eq 1, no-causal-mask + multi-filter intuition), A.2 pre-norm + residual stream rationale, A.3 GELU MLP (mix-then-transform rhythm), A.4 CLS-vs-patch readout + position-embedding interpolation, A.5 ViT→CLIP/SigLIP projection head (drop-in equivalence). Fixed two cross-file 'Equation (2)' refs (they point to fundamentals.md §2.3) → descriptive 'the §2.3 encoder block' (lint #11 correctly rejects bare same-doc eq-refs for a cross-file target). **appendix-b-contrastive-infonce.md** (InfoNCE from first principles): acquired CPC (van den Oord 2018, [49]) and read pp.2–4; B.1 the loss-as-N-way-classification (Eq 1, identifying f=exp(sim/τ)), B.2 the optimal score is the density ratio p(t|v)/p(t) (Eq 2, negatives-independent), B.3 the MI lower bound I ≥ log N − L (Eq 3) → the precise reason batch size matters (tighter bound) + the log N cap explaining SigLIP's saturation, B.4 back to CLIP symmetry + SigLIP's trade. Added reference [49].
- **Result**: A+B validated — lint 0/0 each, validate-refs survey-wide 0/0 (119 cross-file links). Appendices done: A, B. Next: C (connectors), D (VQ tokenization), E (unified generation), F (audio/video), Q (reader questions).
- **Findings**: grounding appendix B in CPC (not memory) yielded the load-bearing payoff — the I ≥ log N − L bound makes "more negatives = better" rigorous and ties CLIP's 32k batch + SigLIP's saturation to one inequality. Worth the one extra acquisition.
## Conversation 19
- **Request**: (autonomous run) — write appendices C, D, E.
- **Actions**: **appendix-c-connector-derivations.md**: C.1 Q-Former's three objectives sharing one query set via attention masks (ITC best-matching-query similarity Eq 1 + unimodal mask; ITG multimodal-causal mask; ITM bidirectional mask + per-query binary head); C.2 Perceiver Resampler as cross-attention (latents=queries, [X;L]=keys/values, fixed-M output, video flattening); C.3 gated cross-attention in full (4-step block, per-layer zero-init tanh gate, deep-fusion-vs-bottleneck framing). **appendix-d-visual-tokenization.md**: read VQGAN (esser-vqgan pp.3–4); D.1 straight-through estimator (Eq 1 gradient-copy + the z_q=z_e+sg[e−z_e] one-liner, bias note); D.2 EMA codebook = online LBG vector-quantizer (descriptive, no memory formulas — VQ-VAE defers EMA to its appendix); D.3 VQGAN perceptual+adversarial min-max objective (Eq 2) + transformer prior over codebook indices. **appendix-e-unified-generation.md**: E.1 Chameleon joint mixed-vocab AR likelihood (Eq 1) + QK-norm stabilization; E.2 Transfusion two-objective + the causal-text/bidirectional-intra-image mixed mask + interleaved AR/diffusion generation; E.3 Janus decoupled-encoder single cross-entropy + three-stage training. Equations C-1, D-1, D-2, E-1.
- **Result**: C+D+E validated — lint 0/0 each, validate-refs survey-wide 0/0 (133 cross-file links, eq tags in 13 files). Appendices done: A, B, C, D, E. Next: F (audio/video), Q (reader questions), then Phase 5 gates.
## Conversation 20
- **Request**: (autonomous run) — write appendices F and Q (final synthesis files).
- **Actions**: **appendix-f-audio-video.md**: F.1 Whisper encoder end-to-end (mel→conv-stem stride-2→sinusoidal-pos→bidirectional transformer; the 100→50→~25 Hz frame-rate cascade as the audio token-budget tradeoff), F.2 discrete audio tokens (AudioPaLM k-means→~1k tokens→embedding-matrix extension; the text-competence-transfer explanation), F.3 video token-budget arithmetic (Eq 1 N_video = F·N_v^frame; the 8×576=4608 worked figure; the three §7.2 levers each attacking a factor). **appendix-q-reader-questions.md** (R-SURVEY explainer Q&A): Q.1 why frozen encoder, Q.2 why high-res costs tokens, Q.3 discrete-vs-continuous generation what-breaks, Q.4 why early-fusion beats cross-attention (+ Idefics2 ablation), Q.5 M-RoPE + native resolution — each a compressed body result with a cross-ref. 1 equation (F-1).
- **Result**: F+Q validated — lint 0/0 each, validate-refs survey-wide 0/0 (155 cross-file links, eq tags in 14 files). **ENTIRE SURVEY DRAFTED: §0–§13 + appendices A–F + Q + index + references.** Next: Phase 5 gates (/check-survey full mechanical gate, citation-audit, cross-link sign-off, final report).
## Conversation 21
- **Request**: (autonomous run) — Phase 5 gates + sign-off.
- **Actions**: Ran the full mechanical gate (/check-survey equivalent): validate-refs survey-wide 0 errors (32 eq markers/anchors, 155 cross-file links, tags correct in 14 files); check-citation-sources 49 entries / 0 errors (41 strong local / 8 weak); renumber eq/sec/para --check clean across all 23 files; lint-math 0 errors across all 23; bare-refs clean at severity=error. **Citation audit**: load-bearing citations were personally read from source at authoring time (citation-integrity prevention); re-read the Qwen2.5-VL Table 3 to verify the SOTA snapshot rather than trust the ledger; spot-verified catalog-tier ledger-sourced claims (PaliGemma linear/prefix-LM, Idefics2 pooling/cross-attn-ablation) against local PDFs — all confirmed; closed-model figures flagged third-party-compiled/[contested], weak tags carry no load-bearing claim. **Cross-link**: crosslink.py IS present; check reports only low-cosine candidates (≤0.187) on an already-densely-linked survey (155 secxref) — filed `todos/2026-06-28-multimodal-llms-crosslink-polish.md` per the rule's todo-allowance. Wrote the implementation report `reports/2026-06-28-multimodal-llms-survey-build.md`.
- **Result**: **SURVEY COMPLETE.** All Phase-5 gates green. 23 files, §0–§13 + appendices A–F/Q, 49 references (41 strong), 33 equations, 155 cross-file links, 45 source PDFs tracked. Tasks #11 (Phase 4) + #12 (Phase 5) done.
- **Findings**: acquiring 16 primaries on-demand during synthesis (not relying on the Phase-3 ledgers for cited values) was the single highest-leverage rigor decision — it turned "tier-A ledger says X" into "read X from the source," which is exactly what citation-integrity's reproduce-from-source clause requires and what made the citation-audit a formality rather than a rework.
<!-- LOG-END -->
