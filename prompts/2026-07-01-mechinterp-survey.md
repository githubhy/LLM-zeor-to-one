# Conversation Log — 2026-07-01 — session: mechinterp-survey

## Conversation 1
- **Request**: `/deep-research-survey proposed,scale:wide` on "various methods in Mechanistic interpretability on various models, on a dedicated branch". Effort: ultracode (xhigh + workflow orchestration).
- **Actions**: Created branch `survey/mechanistic-interpretability`. Read Phase-1/2 skill files + global/phase-1/phase-2 addenda + operational-scale + audience-register configs. Scaffolded `surveys/mechanistic-interpretability/_scratch/` with `brief.md` (north-star research brief) and `outline.md` (24-file section manifest, R-GOV depth tiers, R-CARD method-card plan, R-SURVEY artifacts, ~14 Phase-3 evidence clusters of ≤3–4 Q each). Presented P0-3 brief+outline gate to user with AskUserQuestion (audience register + depth emphasis).
- **Result**: Awaiting single P0-3 confirmation before spending Phase-3 evidence budget. Next: Phase-3 evidence via hardened Workflow agents (wide scale), Phase-4 main-thread synthesis, Phase-5 report + `/check-survey` + citation-audit gates.
- **Findings**: Repo's `multimodal-llms` survey is the closest structural precedent (also `proposed·wide·learner`) — reused its index/notation-contract/depth-legend shape.

## Conversation 2
- **Request**: (P0-3 confirmation) User selected register/depth/breadth via AskUserQuestion.
- **Actions**: Confirmed **Practitioner · Balanced · Toy→frontier+vision-roots**; locked into `_scratch/brief.md`. Launched **Phase-3 evidence Workflow** `wf_8d9c6b10-882` (`mechinterp-evidence`): 14 hardened Sonnet clusters (foundations, superposition, probing-lenses, patching, auto-circuits, saes-core, sae-variants, transcoders-graphs, steering-repe, model-editing, circuits-cases, evaluation, sota-debate, applications-safety) → file-first `_scratch/ev-<id>.md`, exact source hints/no-Glob, WebFetch≤2, ~3–4 Q each, empty-return-as-death + bounded escalating retry (attempt-2 trimmed, resume-from-checkpoint), telemetry captured for footer.
- **Result**: Workflow running in background; awaiting completion notification. Next: acquire confirmed sources via `source-fetch` → Phase-4 main-thread synthesis (scaffold order.json + 24 files) → Phase-5 report + `/check-survey` + citation-audit.
- **Findings**: Evidence agents on Sonnet (breadth-extraction = canonical Sonnet fan-out case per CLAUDE.md); synthesis+verify held on main-thread/Opus.

## Conversation 3
- **Request**: (auto-resume on Phase-3 completion) Triage evidence, proceed to acquisition + synthesis.
- **Actions**: Phase-3 Workflow completed **14/14 clusters alive, 0 dead** (evaluation cluster died once on API-Overloaded, recovered at retry attempt-1 — retry policy worked as designed). 15 agents, 1.30M tokens, 320 tool-uses, ~10.4 min. Read all 14 `ev-*.md` ledgers: gold-grade, source-traceable, disciplined confidence flags; correctly quarantined garbled numbers (spurious arXiv:2605.29358 for Scaling-Mono; IOI "1.1%" arithmetic error vs 26/144≈18%; "13,100"→131,072 OCR artifact; ROME 6.6%-vs-8.7% AIE unresolved). Recurring pattern: many exact numerics are search-snippet-derived and flagged for primary-source verification → drives the acquisition step.
- **Result**: Proceeding to source acquisition (citation-integrity) → skeleton scaffold → Phase-4 synthesis.
- **Findings**: Evidence surfaced the load-bearing quantitative spine already: TopK joint scaling-law constants (Gao Eq.3), ROME closed-form rank-one update, Hase R²=58.5%/+0.03 localization critique, Gemma Scope >400 SAEs/>30M feats, Scaling-Mono 34M/~12M-alive, the 2025 SAE-deprioritization debate (AxBench/DeepMind), circuit-faithfulness non-robustness (Miller COLM'24). "Various models" spine well-covered: toy→GPT-2→Pythia→Gemma2→Claude3 + vision roots.

## Conversation 4
- **Request**: (continuation) Acquire sources, synthesize survey, run gates.
- **Actions**: **Source acquisition** — fetch-corpus.sh + supplementary + Marks = **63/63 arXiv PDFs into `download/`, zero failures** (transformer-circuits.pub/distill/blog = 18 `(web)` tags). **Phase-4 synthesis (main thread)** — wrote all 24 files: index (notation contract, R-GOV legend), exec summary (claims→evidence spine), intro/scope, fundamentals (headline: residual stream, QK/OV, LRH, superposition), methodology+taxonomy (2-axis + method-family table), 5-file method inventory (R-CARD cards: observational/causal/dictionary/steering-editing/automation), circuits-across-models (induction/IOI/grokking/greater-than + universality), evaluation (faithfulness non-robustness, self-repair, SAE metrics, SAEBench/RAVEL), comparison matrix + selection table, SOTA + SAE debate + quant table, applications (safety/editing/steering + maturity verdict), design guidance, open problems + RIS handoff, appendices A–E (full derivations incl. ROME Lagrangian, superposition phase diagram, grokking Fourier, SAE shrinkage/STE, attribution-graph replacement model) + Q (8-item reader Q&A), references (81 entries). Filed `todos/2026-07-01-mechinterp-ris-handoff.md` + `-crosslink-polish.md`. **Mechanical gate** — built references.md, ran link-references --init / renumber-{equations,sections,paragraphs} / validate-refs; fixed 6 secref→secxref, 3 cite-at-line-start joins (wrapped-framing-para hazard), 1 stray para anchor, `>99%`→"over 99%" spurious-blockquote, 3 uncited-ref wirings (incl. DAS [43] content miss). **`/check-survey` all 8 steps GREEN**: 36 eqs sequential, 81 refs (63 local + 18 web), 0 errors, bare-refs pass. Cross-link check → 19 advisory candidates → todo filed (218 links already present).
- **Result**: Survey complete + mechanically green (24 files, ~25k words). Citation-audit workflow `wf_f9750bd8-8f4` running (10 Sonnet verifiers over load-bearing numerics vs local PDFs). Next: incorporate audit verdicts, write Phase-5 report.
- **Findings**: The wrapped-framing-paragraph hazard: bare `[N]` at a wrap-point is lint-clean until `link-references --init` promotes it to a `<!-- cite -->` comment, which then trips the line-start-comment rule — so the single-unwrapped-line discipline must apply from first authoring, not retrofitted. A literal `>` before a digit at a wrap-start silently becomes a blockquote and derails renumber-paragraphs.

## Conversation 5
- **Request**: (continuation) citation-audit + sign-off.
- **Actions**: **Citation-audit** Workflow `wf_f9750bd8-8f4` (10 Sonnet verifiers vs local PDFs, 25 checks): **21 verified**, 2 real MISMATCH corrected, 1 false alarm. Corrected **ROME §5.1** (8.7% is individual-state peak; MLP-specific is 6.6%, attention 1.6% — not "8.7% MLP") and **RepE §7.2 LoRRA** (+11.3/+11.6 pp, not the secondary-sourced +6.6/+13.1); both re-verified in-process against the PDFs. **Arditi NOT_FOUND was a false alarm** — shared-`/tmp/paper.txt` race between concurrent verifiers; file re-confirmed correct (Refusal/Arditi, 13 models/72B). Filed `bugs/2026-07-01-01` (2 value drifts, med/fixed), `field-notes/2026-07-01-mechinterp-survey` (wrapped-para + `>`-blockquote + temp-race lessons). Re-ran gate → still GREEN. Wrote `reports/2026-07-01-mechinterp-survey-report.md` (deliverable, telemetry, audit, footer).
- **Result**: **Survey COMPLETE, green, audited, corrected, reported.** 24 files / ~25.4k words / 36 eqs / 81 refs (63 local + 18 web). Not committed — pending user go-ahead. Follow-ons tracked in 2 todos.
- **Findings**: Flag-then-audit paid off — both corrected values were exactly the ones the evidence ledger had pre-flagged as secondary-sourced. Parallel-verifier briefs must use per-agent temp paths; a shared scratch path manufactured a false NOT_FOUND.

## Conversation 6
- **Request**: "commit this and open a PR".
- **Actions**: On dedicated branch already. Staged 116 files (45 survey + 63 LFS PDFs + 2 bugs + 3 todos + 1 field-note + 1 report + 1 prompt log); **excluded the pre-existing unrelated `prompts/2026-06-28-qkv-index-notation-fold.md`**. Committed `28b497a`. First push rejected (GH008 — 63 PDFs are Git-LFS-tracked, blobs not uploaded); ran `git lfs push --all origin <branch>` (159 objects, 720 MB), retried push — **pre-push gate clean, pushed OK** with upstream tracking. Opened **PR #1** against `main`.
- **Result**: https://github.com/githubhy/LLM-zeor-to-one/pull/1 — survey shipped for review. Session complete.
- **Findings**: Repo tracks `download/*.pdf` via Git LFS (`.gitattributes filter=lfs`); a plain `git push` references LFS pointers but the remote rejects until `git lfs push --all` uploads the blobs first — do the LFS push before the branch push when a commit adds new PDFs.

## Conversation 7
- **Request**: "the /cross-link polish pass".
- **Actions**: Invoked `cross-link` skill on `surveys/mechanistic-interpretability`. Stage 1 extract (79 sections). Stage 2 candidates (19 at cosine ≥0.15, 1 batch). Stage 3 judge = **1 Sonnet agent** (`wf_561f4f8b-d97`), structured output: **kept 13/19**, correctly rejected the 6 redundant/backwards appendix↔main pairs. Fixed anchor_phrases to real normalized source text (I'd paraphrased the judge's snippets). Stage 4 apply: **12/13 landed** (1 dropped, anchor-not-found on the weakest E.1→Q.5). Stage 5: full gate re-verified GREEN (apply edits bytes directly, no PostToolUse hook — ran lint-math/sections/paragraphs/link-refs/validate-refs/bare-refs manually). Closed `todos/2026-07-01-mechinterp-crosslink-polish` (Resolution + INDEX). Committed `b8dfa1a`, pushed → PR #1 updated.
- **Result**: 12 high-value links added (5 main→Appendix-Q reader-Q&A back-links + 7 assertion→derivation forward links to A/B/C/D/E). Survey now **230 valid cross-links**, gate green. Residual 14 advisory candidates triaged as redundant. Done.
- **Findings**: When prepping a judge batch, pass the tool's REAL normalized snippets (from `crosslink.py candidates`), not hand-paraphrased ones — the judge's verbatim anchor_phrase must match the normalized file for `apply` to place it (1 link was lost to this). `apply` edits bytes without triggering the PostToolUse lint hook, so Stage-5 verification must be run by hand.

## Conversation 8
- **Request**: "apply the links and update the PR" → (clarified) "add the dropped E.1 → Q.5 link".
- **Actions**: First verified the 12-link polish was already applied/committed(b8dfa1a)/pushed to PR #1 (no redundant re-do). Then added the 13th link (§E.1→Q.5) that `apply` had dropped on an anchor mismatch — placed manually after "a first-order approximation otherwise" with the inline `secxref` style, lint-clean. Verified Q.5 resolves + gate green (231 cross-links). Updated the crosslink-polish todo resolution (all 13 kept links now live). Committed `e9f4857`, pushed → PR #1 (3 commits). Pre-push gate clean.
- **Result**: All 13 judge-kept cross-links live; PR #1 current. Survey fully signed off.
- **Findings**: For a single follow-up link, a manual Edit (I control placement + get the PostToolUse lint hook for free) is cleaner than regenerating the crosslink candidates/decisions just to re-run `apply`.

## Conversation 9
- **Request**: "start the SAE frontier reproduction study" → (mid) "what are the RIS options?" → "go ahead end-to-end automatically… commit/push/PR at end."
- **Actions**: Ran `reference-implementation-study` (**proposed** mode, all 13 items) end-to-end for RIS candidate 1 (SAE fidelity–sparsity frontier). Confirmed scope via AskUserQuestion: proposed · **synthetic-oracle + GPT-2-small** (Gemma-2-2B infeasible on 17GB Mac — decision 2026-07-02-01). Built `implementation/sae_frontier/` (4 SAE variants ReLU/Gated/TopK/JumpReLU + synthetic superposition generator + GPT-2 harvester + metrics + oracle + deterministic trainer + registry), 56 tests. **G1** (15/15, oracle P0-5 + determinism P0-1 + registry P2-1), **G2** (S1 frontier, 5 paired seeds, 95% CI, P0-2 pairwise significance, P1-4 profiling), **G3** (H4 width sweep + robustness + manual-Morris P0-3), **G4** (precision DoE P2-3), REPORT gate — all PASS. Wrote full 14-section report `docs/sae-frontier-implementation-study.md` (P1-2 grid, P2-2 reproduce.py, P2-4 Pareto). Filed bugs 2026-07-02-01/02, decision 2026-07-02-01, todos sae-frontier-followups + parent-handoff update.
- **Result**: **H1–H4 all confirmed. TopK recommended** (EV@L0=8: relu 0.31 < gated 0.67 < jumprelu 0.76 < topk 0.82, all p<1e-6; TopK best feature recovery 0.74; gap grows with width R; int8 near-lossless). S2 real-model run deferred (CPU-impractical, explicit n/a). Committing → PR #1.
- **Findings**: (1) JumpReLU STE bandwidth 1e-3 was too small for O(1) acts → threshold gradient-starved → dense collapse (L0≈48), initially made it a false winner; fixed to 0.1 (bug 2026-07-02-01) — a real reproduction pitfall. (2) The synthetic-oracle substrate is *stronger* than Gemma-scale in one respect: ground-truth feature recovery, which no real-model study can measure. (3) Morris: SAE absolute quality is training-limited (lr/steps ≫ width/data) — the matched-L0 ordering is robust, the level is not.

<!-- LOG-END -->
