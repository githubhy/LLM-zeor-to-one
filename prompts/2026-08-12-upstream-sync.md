# Conversation Log — 2026-08-12 — session: upstream-sync

## Conversation 1
- **Request**: `/sync-upstream from ../data-channel-receiver`, immediately re-issued as `/sync-upstream from the main branch of ../data-channel-receiver` — inbound sync of harness config + the viewer application from the upstream template repo, re-adapted telecom/3GPP → LLM/AI.
- **Actions**:
  - **Scope resolution (the load-bearing step).** Two traps, both caught before porting: (a) the upstream working copy is *live* — a first read of `HEAD` landed on `plan/tr38901-calibration-campaign` because a concurrent session moved it, and diffing that branch would have imported an unmerged campaign-execution harness; (b) upstream's **local `main` was 1647 commits behind `origin/main`**. Fetched and scoped to `origin/main` = `cedfccb2`. Final range `9b118d3..cedfccb2` = **162 files / ~20,287 insertions** across 245 config+viewer commits. Skipped `444aeb0d` and `710afc6f` via the round-trip guard (this repo's own sync-backs, PRs #36 and #15).
  - **Two scope calls put to the user** (both would have led to materially different work): the ASIC hardware-realization family → *re-domain to GPU/accelerator*; `spec-provenance` → *re-domain, drop the 3GPP fetcher*.
  - **`viewer/tools` (60 files).** Copied from `origin/main`, then a two-pass domain scrub (97 → 0 leakage hits). 303/303 tool tests pass.
  - **Viewer app.** `highlight-shared.js` was already byte-identical (our sync-back round-tripped cleanly). Took upstream's `viewer.js` (shared-lib consumption, footnote in-situ peek, exact DOM-order highlight disambiguation, scroll-inside-popover fix), `serve.js` (stat/read-race hardening on top of our EISDIR fix), `style.css`, and the test deltas — then **re-applied the local-only relative-asset-link fix** upstream lacks. 314/314 viewer unit tests pass.
  - **Config/gates.** ~20 severity toggles; `crosslink-scope` converted to named `[group]` form (`llm-methods`, derived via `crosslink-cluster.py propose`); keep-out + rejection ledgers; rebuilt `.githooks/pre-push` (~25 gates) preserving the local **git-lfs chaining** and Windows python resolution; new `.githooks/pre-commit`; `SessionStart` hooksPath install + `PreToolUse` foreground-background guard; shared hooks moved from the machine-local settings file into the **tracked** `settings.json` (and de-duplicated from the local one).
  - **Rules.** 4 new (`calibration-residuals`, `cross-language-port`, `release-documentation`, `reset-durability`) + 5 surgically merged. `sim-report-completeness` gained three emphases re-domained to published-benchmark scores; `workflow.md` gained PLAN-REDFIRST / UTF8-WRITE / BG-RUNINBG / one-live-writer / MATH-BASIS / MATH-REDERIVE.
  - **Skills.** 3-way merged all 30 files with `git merge-file --diff3` — 12 clean, 8 conflicts resolved by hand. Notable correction adopted: `survey-explainer-fold` now requires math exhibits as **untagged `$$` KaTeX**, reversing our inherited "ASCII art in a fence" guidance whose cascade rationale upstream proved false. New: `harness-harvest`, `results-reconciliation`, `upstream-pr`. Rewritten: `spec-provenance` (arXiv-version / model-card / harness-release ladder), `kernel-bringup` + `accelerator-cost-study` (from the ASIC pair).
  - **Catalogs.** `CLAUDE.md` hooks/rules/skills/commands sections + `AGENTS.md` rewritten to the pointer form; `/normalize-survey` added; `/check-survey` extended to 15 steps.
- **Result**: Pre-push gate **green** (exit 0); 303/303 tool tests; 314/314 viewer unit tests; leakage grep clean across `CLAUDE.md`, `AGENTS.md`, `.claude/`, and every file this sync touched. Mark advanced to `cedfccb2`; `pending_sync_back` cleared (both PRs merged). Filed `decisions/2026-08-12-01` and `todos/2026-08-12-upstream-sync-followups.md`.
- **Findings**:
  - **The new gates found real defects in our own corpus, not just upstream's.** 10 cross-file equation refs in `surveys/mechanistic-interpretability` were marked `<!-- ref: -->` (same-file) while pointing at siblings — a genuine blind spot: not an orphan so `renumber-equations` was silent, not an `xref` so it would never propagate. Also: 2 wikis with no `sec-` anchors (link sources but never targets), and 11 unresolvable record refs (5 pre-existing since bootstrap).
  - **An imported gate was itself broken here.** `check-record-ids.py` scanned with `rglob("*.md")`, sweeping in gitignored build output — Playwright's `test-results/**/error-context.md` copies a failing test's *source comment* (bug IDs included) into a generated file, so merely **running the viewer suite** made the gate fail the push. Fixed to scan git's tracked set, inheriting `.gitignore`, with a filtered-rglob fallback.
  - **Corpus-calibrated tests do not port literally.** `test_corpus_scope.py` asserted `n > 100` against upstream's 811-file corpus and failed here at 78 — testing the corpus, not the tool. Rewrote the floor as corpus-derived so the property (*does the scan get past the root?*) survives any corpus size.
  - **A domain-specific registry left unadapted is worse than no gate.** `check-basis-declarations.py` shipped an ADC/analog symbol registry and fired noise on our corpus. Re-domained to real LLM basis ambiguities — `N` non-embedding vs total, `D` unique vs seen tokens, `B` sequences vs tokens, `pass@k` sampling basis, and loss in **bits vs nats** as the direct analogue of the dB-reference check. It now reports a genuine 42-item backlog at `warn`.
  - **Genericizing has a limit: a measurement record.** `bench/.../RESULTS-2026-05-30.md` names the topics its experiments actually ran on. Rewriting those to LLM topics would falsify a record rather than re-domain a fixture — repaired its dangling refs, left the measured topics, documented the grep hit.

## Conversation 2
- **Request**: "For things you not imported or changed, check them again" — re-verify the not-imported / deviated set.
- **Actions**: Re-derived the full 162-file delta and diffed it against the working tree instead of trusting the session's own notes. Four outcomes:
  1. **One genuine miss**: `bench/method-eval-ris-hardening/` (the A/B backing `[opt:ME-ADVERSARIAL-METRIC]`) — and `.claude/skill-options.json` already cited it, so the registry carried a dangling path. Re-domained (saturating logit bias → mean-confidence metric maxed while mutual information collapses) and **run**; it reproduces upstream's numbers exactly, since only the labels changed.
  2. **A defect introduced by this session**: the `_SPECNUM_RE` comment in `check-citation-sources.py` was rewritten to claim IETF/RFC coverage while the regex still matched only the 3GPP alternation. Fixed the code to match the claim (`\bRFC\s?\d` added), noted that `_ARXIV_RE` owns arXiv IDs (197 of 229 entries), unit-checked six cases.
  3. **Three severities promoted `warn` → `error`** after measuring each at `error` rather than guessing: `crosslink` (rc=0 — it gates the 0.30 block-score, and the open candidates are 0.275/0.220/0.208), `derivation-dag` (rc=0 on all four surveys), `reproduce-blocks` (rc=0). The three that genuinely block stay at `warn`.
  4. **Claims that held, now evidenced**: all 9 "upstream branch-only" files confirmed absent from `origin/main` and present on the branch; `bench/reference-implementation-study/` never ported at bootstrap and already documented as graceful degradation in the RIS skill; `power-gate-severity` has no referencing gate.
  Added two regression tests pinning the `check-record-ids.py` scan-scope fix (generated output not scanned; an *authored* dangling ref still fails).
- **Result**: 305/305 tool tests; pre-push gate green with the promoted severities; leakage clean. Committed as the follow-up to c441770.
- **Findings**:
  - **The blanket rollout heuristic was weaker than the evidence.** "New gates start at `warn`" is a reasonable prior, but measuring each gate at `error` showed three were already non-blocking — so `warn` was giving up real protection for nothing. Measure, then set the toggle.
  - **`error` on the cross-link gate does not mean "no gaps allowed."** It gates the **block-score** (0.30), not `--min-score` (0.20). Reading the severity without reading which threshold it gates is how a team talks itself out of a gate it could already afford.
  - **Re-domaining can introduce the exact defect it is cleaning up.** Rewriting a comment to match the new domain, without touching the regex under it, produced a comment that lied about its code — the same class as the wrong-but-resolving citations `citation-audit` exists to catch. A re-domain pass should diff *code* separately from *prose*.

## Conversation 3
- **Request**: "clear the 3 cross-link gaps".
- **Actions**: Ran the `cross-link` skill over the `llm-methods` group. Shortlist was 3 pairs — far under the ~200 judge threshold — so the **human review-sheet path**, not the agent judge. Read both source and target of each pair in full before deciding; all three were `link` (none rejected, so the rejection ledger stayed empty). Overrode all three default anchors: C002's would have split "speculative | decoding" mid-phrase, C003's was the math-stripped form and sat between a method and its citation. Applied with `--reviewed-by`, verified against `git diff` rather than the tool's report.
- **Result**: `crosslink.py check` → **no cross-link gaps**. validate-refs 0/0 on both surveys, lint-math 0/0 on all three edited files, 305/305 tool tests (76 crosslink), full pre-push gate green. Updated the now-stale gap counts in `.claude/rules/cross-linking.md` and `CLAUDE.md`, and marked todo item 2 cleared. `temp/` removed (it is not gitignored).
- **Findings**:
  - **A tool defect surfaced only at the point of use.** `crosslink.py::short_text` hard-truncated titles at 6 words with no ellipsis, turning "A Discovered Circuit: Reverse-Engineering a Real Model" into link text ending "…a Real" — which reads as a dropped word, not an abbreviation. Fixed to truncate at the subtitle colon when the head is usable (≥2 words), else fall back to the word cap *with* an ellipsis. Four regression tests added.
  - **`link_markdown` is frozen at Stage 2, not recomputed at apply.** Fixing `short_text` changed nothing until the candidates JSON was regenerated — the applier replays the stored string. Worth knowing: a tool fix mid-pipeline requires re-running `candidates`, not just `apply`.
  - **The default anchor is a starting point, not a recommendation.** Two of three would have damaged the prose. The skill's "edit the Anchor" affordance is load-bearing, not decorative.
  - **Two of the three gaps were prose pointers that already existed** — §10.3 literally ended "quantified in Section 14", and §2.2 discussed composition while linking A.2 and A.4 but not A.3. The detector was finding places the author had already gestured at a link and not made one, which is the highest-value class it can find.

## Conversation 4
- **Request**: "make the sync back a todo".
- **Actions**: Verified each candidate against `origin/main` rather than filing from memory — all three confirmed still absent upstream: `check-record-ids.py` still scans with `rglob` (line 180), `crosslink.py::short_text` still truncates at 6 words, `viewer.js` has 0 occurrences of the relative-asset-link guard vs 2 here. Filed `todos/2026-08-12-sync-back-generic-tool-fixes.md` with the three, plus an explicit *not in scope* section for the three near-misses (the `RFC` regex widening and the re-domained registry are `SKIP-domain`; the corpus-derived test floor is offer-in-PR-body, not bundle-silently). Also **closed the stale `2026-07-05-land-viewer-fixes-upstream-via-back-sync` todo** — still `in-progress`, but its acceptance was already met: PR #36 is an ancestor of upstream `main` and the fixes are present there. Updated both INDEX rows.
- **Result**: `check-record-ids` OK (INDEX/file consistency intact across both edits). One todo closed, one opened.
- **Findings**:
  - **A todo can outlive its own acceptance.** `2026-07-05` sat at `in-progress` while the PR it was waiting on had merged; nothing closes a todo automatically, and this session's own sync had already advanced the mark past it. Worth checking open todos against reality at the end of a sync, not just filing new ones.
  - **Rank the sync-back by who it hurts.** The `check-record-ids` fix is the highest-value of the three because it breaks *upstream's own push*: running their viewer suite writes bug IDs into a gitignored `test-results/` file that the gate then reads. They are more exposed than this repo, not less.
  - **The asset-link fix has already survived one sync by luck.** It persisted through the 2026-08-12 inbound only because it was deliberately re-applied after taking upstream's `viewer.js` wholesale. Left unsynced it needs re-applying by hand on every future viewer sync — the exact drift `--back` exists to stop.

## Conversation 5
- **Request**: "What I wanted to learn before in this repo?", then "link the two unreachable wikis from their survey sections", then "commit, push".
- **Actions**: Answered the recall question from artifacts (memory files + survey scoping choices in the earliest `prompts/` logs + the 12 `docs/` studies), not from reconstruction. Then cleared the reachability gap: had `crosslink.py candidates --min-score 0.05` propose hosts, read each wiki and each candidate host, and linked both from the section each actually supports. Promoted `.claude/reachability-severity` `warn` → `error` now that the backlog is zero.
- **Result**: `crosslink.py reach` → **2 reachable, 0 UNREACHABLE**; passes at `error`. lint-math 0/0 on both edited files, validate-refs 0/0, link-fragments 992/0 dangling.
- **Findings**:
  - **The top TF-IDF host was the wrong home.** For the MI coverage-gaps wiki the tool ranked §I.4 (*The Intervention Toolkit*) highest on vocabulary overlap — but the wiki is the gap analysis that *recommended adding* that toolkit and produced Appendix I wholesale, so a link there would have read as "for more on interventions, see…". Its home is the appendix opening, §I.1. This is exactly the keep/**where** judgment the pre-filter cannot make.
  - **`make_link`'s default label is poor for wiki targets.** It uses the target *section's* title, and these wikis' section titles are sentences ("The two limits are not the same limit"), so the label named neither the document nor the topic. The rule's own example uses *descriptive text* (`[softmax derivation](...)`), so the form (plain link, no glyph, `../../wikis/` path) came from the script and the label named the document.
  - **Caught myself putting a cross-reference inside a figure caption.** The first F.2 insertion landed mid-caption, between "The memory wall." and "Training state is…", which breaks the 4-section caption schema. Moved to the body paragraph. Captions are not general prose.

## Conversation 6
- **Request**: `/mattpocock-skills:grill-with-docs "the learning road"`, then three rounds of answers — "a+b, rent if ~5 USD for one experiment, 10 times to understand a topic, which at least lasts a month"; "4, 5, 6 all agreed"; "7, 8, 9 all agreed".
- **Actions**: Ran the grilling skill over three rounds (9 questions, each with a recommendation; all 9 accepted). Fact-gathered from artifacts rather than reconstructing: `todos/INDEX.md`, the umbrella todo, `implementation/` tree, and a grep for where the induction phase change is actually claimed in the corpus. Landed the outcome as `CONTEXT.md` (new — the repo's first glossary, previously absent by design per `docs/agents/domain.md`) + `decisions/2026-08-13-01-learning-road.md`; closed `todos/2026-07-03-blocked-backlog-consolidated.md`; annotated `tiny-transformer-gpu-host-rungs` (first topic + resumability precondition + exit condition) and `gpt2-training-reproduction` (held behind the shrink); updated 3 `todos/INDEX.md` rows and the `decisions/INDEX.md` row.
- **Result**: Six calls settled — a topic is one study-sized falsifiable question (a survey is a prerequisite, not a topic); ~10 experiments at ~5 USD, >=1 month; depth-first with surveys pulled on demand; GPT-2 124M shrunk to the 10M rung; a topic is *understood* when the survey section cites your own measurements in place of the claim it cited; a rented host is a compute worker, never a repo clone. First topic selected with a named precondition that precedes any spend.
- **Findings**:
  - **A blocker outlived its truth by six weeks.** The umbrella parked five RIS study follow-ups as "another session's studies — not ours to modify"; PR #2 merged 2026-07-02 and `implementation/` on `main` carries all eight study packages. Nothing re-reads an umbrella when the world changes underneath it — the same failure shape as Conversation 4's todo-outliving-its-acceptance, now twice in one session. The fix was to close it rather than rewrite it: a second consolidated index beside `INDEX.md` is what drifts.
  - **The survey had already specified the experiment.** Grepping for where the phase change is claimed found `appendix-a-qkv-first-principles.md` Claim 2 stating it is *"an empirical claim, not a theorem, and exactly what a from-scratch reproduction can check: overlay the ICL-score-versus-step curve on an induction-head-strength-versus-step curve."* The exit condition did not need inventing — past-you wrote the acceptance test into the survey and it had been sitting there unexecuted. This is the strongest argument for the exit condition chosen: the corpus already knows what it is missing.
  - **The two halves of the road are one artifact, not two schedules.** "Curriculum + capability ladder" reads as a conflict (breadth vs depth, and at one topic/month they cannot both run) until the exit condition fuses them — the ladder produces the measurement, the curriculum absorbs it by editing the survey that raised the question. That reframing is what made depth-first safe to choose.
  - **The per-experiment budget is a design constraint on the code, not just on the wallet.** ~5 USD slices mean a driver that cannot resume at zero recompute cannot be run at all — so the resumability audit is the first task of the month and precedes any spend. The cheap check on any resumed artifact is that its log's progress counter is strictly increasing.

## Conversation 7
- **Request**: "commit and push" (the learning-road artifacts), then "run the resumability audit on run_phase3.py".
- **Actions**: Committed + pushed `5fc430a` (gate green, `fce2b61..5fc430a`). Then read `run_phase3.py`, `model.py::train_toy`, `config.py`, and `data.py` and audited against the precondition (`.claude/rules/workflow.md` flush-and-resume + one-writer-per-checkpoint). Filed `bugs/2026-08-13-01` (high) and `todos/2026-08-13-phase3-resumability-hardening.md`; annotated the first-topic todo with the audit result and both INDEX rows.
- **Result**: Precondition **half passes**. Single-writer is satisfied by construction. Three gaps block spend: config-blind resume key (silent wrong output), no intra-run checkpointing, and no Rung-2 implementation.
- **Findings**:
  - **I over-extended the evidence when recommending the first topic.** I argued Rung 2 was the right start because "`implementation/tiny_transformer/` already has `run_phase3/4/4b/5` and ran end-to-end on CPU — so rented hours buy compute, not bring-up." That is true of **Rung 1**. `build_toy` is the only model factory, all four `PRESETS` are toy-scale, and `data.py` is synthetic-only with no text corpus loader — so **Rung 2 has no code**. The pick still stands (it is the cheapest scale that can answer the question), but its first weeks are local bring-up, not rental. The lesson is narrow and repeatable: existing-code evidence gathered for one rung was carried to a sibling rung without re-checking.
  - **The silent bug fires on precisely the transition we chose.** `run_phase3.py` resumes on `(n_layers, seed)` and ignores `CFG`. Editing `CFG` reduced→full skips every already-trained seed, and `aggregate()` stamps the pooled summary with the *current* config. Each seed's own config **is** written to disk and never read back — the information to detect it exists and nothing consumes it. Reproduces perfectly on every run, which is the `calibration-residuals` check-6 shape: the number was never wrong, the story about it was.
  - **A checkpointing gap can hide behind a small config.** `train_toy` has no checkpoint write in its loop at all. At 800 steps that is invisible; at 20k steps it is the whole billing slice. The reduced CPU pass did not merely under-test the resume path — it made its absence unobservable.
  - **Restoring weights is not restoring a run.** The resume fix has to persist `rng`, `eval_rng`, and torch RNG state alongside model+optimizer, or a resumed run continues onto a different data stream — a silent determinism break rather than a crash. Wrote that into the acceptance as a resumed-vs-uninterrupted agreement check.

## Conversation 8
- **Request**: "Give me a progressive knowledge list then let me know where I am", then — mid-turn — "I used the skill to create all these surveys, but it does not mean I understand them", then "quiz is an easy way. I think it's better to use my questions in history to decide where I am. What do you think?", then "run the extraction".
- **Actions**: Drafted a capability ladder (L0-L7) and folded it into `docs/development-timeline.md`. The user's correction invalidated it, so it was rebuilt with two columns (corpus vs reader) and the reader column recorded as *unmeasured*. The user then proposed measuring from their own question history instead of a quiz; agreed, and ran the extraction: 221 `**Request**` lines from 14 `prompts/` files plus the 23 `> **Note --**` survey folds, each classified OPS/LEARN, then by kind (K1 what-is / K2 how / K3 why / K4 what-breaks-it / K5 what-is-missing) and by rung. Wrote `docs/reader-frontier-2026-08-13.md`; updated both the ladder and the Current Snapshot to cite it. `CONTEXT.md` gained a two-half exit condition.
- **Result**: Reader column measured: L1 engaged-but-unfinished, L2 thin, L4 very thin, L5/L7 not started.
- **Findings**:
  - **Two independent measures agreed.** Rung distribution from the prose classification (76% L1) and from raw fold file-counts (87% L1) were derived separately and converged. Appendices D-I -- the larger half of the corpus -- produced zero folds and few questions.
  - **Zero subject-matter questions in the last three sessions**, across 21 conversations -- the exact window in which eleven studies were executed. The cause is not disinterest: those sessions ran under an explicit "go ahead end-to-end automatically" mandate. **The execution mode is the mechanism.** Understanding is not outsourced by a skill writing a document; it is outsourced by choosing a mode in which no one has to ask anything. This makes execution mode a learning variable, not a speed knob.
  - **The question-quality signal is good where questions exist.** ~22% are K4 (*what breaks it*), and several caught real defects -- a challenged rank claim ("8?"), a challenged dimension convention, a hand-annotated equation submitted for confirmation. The leverage is raising K4 where the record is silent, not raising the total.
  - **The SP framing is self-generated.** The matched-filter mapping, "is it a good idea to map to an *adaptive* filter rather than a fixed matched filter?", and "So LLR is a special case of the logit?" are unification questions from the L0 substrate -- the strongest evidence in the record of reasoning rather than absorption.
  - **The instrument I first proposed was wrong.** `appendix-q-reader-questions.md` is skill-synthesised ("questions the main text raises"), so quizzing on it would have measured the corpus twice. The user's proposal was the better instrument and I should have reached for it first.

## Conversation 9
- **Request**: "what's your recommended ways of continuing the study? Give me 5 best ways among 20", then a rejection of recommendation #1 — "me authoring is much less effective" — with a counter-proposal (agent offers a topic menu, reader picks or substitutes, then reads/asks/folds/experiments with help), then "yes, write it", then "find ways to make the whole learning pattern in a session a command/skill".
- **Actions**: Generated ~20 continuation options, filtered to 5 with reject reasons for the rest. The reader rejected the top pick; checked it against the frontier data, found the rejection was correct, and conceded. Wrote the reader's model into `CONTEXT.md` ("How a study session runs") and filed `decisions/2026-08-13-02`. Then designed the session pattern as harness.
- **Result**: Operating model settled and recorded: read-ask-fold with the reader choosing, one guardrail (zero folds = the session did not happen), menu drawn from the measured frontier, autonomous mode out of bounds inside a topic-month.
- **Findings**:
  - **My recommendation was refuted by the measurement I had just run.** I proposed reader-as-author for ownership; the frontier analysis showed the most productive learning session on record (2026-06-29, 28 questions, 3x any other) was pure read-ask-fold with me doing all the writing. I had mistaken **authoring for the real variable**, which is *who holds the thread* — and the reader holds it by choosing and asking, whoever operates the keyboard. Worth generalising: when a recommendation contradicts data gathered in the same session, the data wins, and I should check before recommending rather than after being challenged.
  - **One measurement supports one guardrail.** I had three ready (quota, question log, answer-delay). Only question-count was ever shown to track frontier movement, so only one guardrail is earned; the rest was over-engineering, and a quota measures volume — never the deficiency.
  - **The menu must be computed, not chosen by me.** The decision forbids drawing topics from "what is easy to explain", but nothing would stop that drift if I pick by feel. That is the same Tier-1/Tier-2 split as the cross-link subsystem: detection deterministic, judgment human — which is what makes the session pattern worth building as harness rather than leaving as a habit.

## Conversation 10
- **Request**: "what can you borrow from the mattpocock-skills for this /study command?" then "yes, all of them" — implement the three recommended borrowings.
- **Actions**: Read all 35 plugin skills' descriptions; `productivity/teach` and `productivity/wait-what` were the only relevant ones. Implemented three borrowings. (1) **Retrieval practice**: `study-frontier.py --recall N` — collects every fold, dates each line via `git blame --line-porcelain`, ranks oldest-first (age *is* the spacing interval); uncommitted folds are not yet due. (2) **ZPD gate**: `.claude/study-prereqs` (glob → prerequisite rung, first-match-wins) + `.claude/study-reader-rung` (`L1`); menu shows `prereq <= reader+1`, held-back documents reported as a count. Both fail open. (3) `.claude/commands/wait-what.md` — re-pitch hatch, adapted (it already assumed a `CONTEXT.md`). RED-first: 8 new tests written and confirmed failing before implementation; 323 tool tests pass (was 315). Updated `study.md` (§1 recall, §2 gate, §3 `/wait-what` pointer), `CONTEXT.md`, `CLAUDE.md`; filed `decisions/2026-08-14-01` + `todos/2026-08-14-sp-transformer-reference`.
- **Result**: The menu is now gated and opens with recall. `appendix-i-mechanistic-interpretability` (previously ranked #2 for an L1 reader) is held back; `appendix-b-kernel-regression-family` — the unread L1 sibling of the two heavily-folded documents — rose into view. Deferred: the compressed SP↔transformer reference document (a synthesis, better written from more folded material).
- **Findings**: Two worth keeping. **(a) The capability ladder is the wrong data source for a ZPD gate.** It tracks *depth of engagement with the same object*; L3 ("the system around the model") is a different axis from L2, not a harder one, so ladder-gating would have hidden perfectly readable body sections. The gate needs *prerequisite* depth — what a reader must already hold — which the ladder informs but does not determine. **(b) First-match-wins order is load-bearing and failed silently on the first run**: the `llms-for-coding/*.md` catch-all sat above the specific `appendix-i` rule and swallowed it, so the one document the gate exists to hold back leaked through at L2. Caught only by reading the live output against the stated intent — the rule file compiled, parsed, and produced a plausible menu. Catch-alls are now last, with the incident recorded inline in the file.

## Conversation 11
- **Request**: "a complete comparison list of /teach, /wait-what, /study", then "borrow more from /teach for /study?"
- **Actions**: Re-read both plugin skills in full and produced a three-way comparison (identity, 22-dimension table, plugin-vs-repo `wait-what` delta, and a borrowed/deferred/rejected status for every `teach` feature). Then took two further borrowings into `.claude/commands/study.md`: **prediction-before-the-run** on the experiment escalation, and **one named primary source per fold**. Recorded as an addendum to `decisions/2026-08-14-01` rather than a second decision (same call, same day, same source).
- **Result**: Two one-clause edits. Everything else from `teach` stays rejected or deferred on the reasons already recorded. Flagged for the reader to decide: the repo's `study.md` / `wait-what.md` carry no frontmatter, so unlike the plugin originals (`disable-model-invocation: true`) both are model-invocable.
- **Findings**: Three. **(a)** The separating axis between `/teach` and `/study` is *who asks*, and it follows from *what already exists*: `teach` assumes an empty workspace so the agent authors and quizzes; here the corpus is complete and skill-produced, so the reader asks and a quiz would have the agent grading its own output. Every other difference is downstream of that. **(b)** I had listed interleaving as "not taken" — wrong. `--recall` serves the globally *oldest* fold, which is almost never from the document the menu then serves, so interleaving is already satisfied by construction. **(c)** The prediction gap was the highest-value item left and it is not a general improvement: L6 (*predict and falsify*) is unstarted in **both** ladder columns, and `/study` had no step that would ever start it.

## Conversation 12
- **Request**: `/study` (no argument), then mid-session "where is the viewer.content.jso" → "yes".
- **Actions**: Ran the study session per `.claude/commands/study.md`. Retrieval practice served the corpus's oldest fold (55 days, `language-models-from-first-principles.md:165`, *"why this is $O(T)$, not $O(T^2)$"*); the reader answered "I don't even know what this is about". Graded it with a rig-vs-signal split rather than as a pure reader finding, then drew the gated frontier menu (4 options, recommendation `language-models-from-first-principles.md`). The reader then asked about `viewer.content.json`; confirmed absent here (upstream-only), wrote it with 7 curated roots, verified all 7 resolve at startup, amended `/sync-upstream` in two places (it had listed the file as out of scope — true until that turn), filed `decisions/2026-08-14-02`. Committed `1d8f017`, pushed.
- **Result**: **The study session did not complete — zero folds.** The reader never picked from the menu; the session was superseded by the viewer work and then by Conversation 13. Reported as zero rather than papered over, per the command's own guardrail. Two defects filed at close as promised: `todos/2026-08-14-recall-prompt-is-a-label-not-a-question` and `bugs/2026-08-14-02`.
- **Findings**: Two, both from *using* tooling shipped hours earlier the same day. **(a) The recall prompt is a label, not a question.** `--recall` prints the fold's bold lead, which is written to sit beside its host paragraph; served cold, "why **this** is $O(T)$" has no antecedent. So the reader's blank is partly a *measurement artifact* — `calibration-residuals` check 6, check the rig's preconditions especially on the easy case. A blank reader looks like a strong finding and is exactly what a broken rig produces. **(b) The pulse check over-counts.** `--since` reported "1 fold added" for a session that added none: fold lines carry their `<a id="p-…">` anchor inline, so a routine `renumber-paragraphs` pass rewrites every fold line and the added-line scan re-counts them. That converts the zero-folds honesty guardrail into a rubber stamp, precisely after routine maintenance.

## Conversation 13
- **Request**: `/deep-research-survey max mode on Large Language Models for Code: From Codex to Agentic Software Engineering`.
- **Actions**: The requested title is verbatim the H1 of an existing survey, so Phase 1 measured it instead of guessing: body = ~11,900 words across 16 sections with **0 equations**, appendices A–I deep but deriving the *general* transformer. Resolved `max` → `proposed` + `scale: wide` + `audience: learner`; gated at P0-3 with a tier table and three branching questions (all three recommendations taken). Phase 3: 8 hardened Sonnet evidence agents (~21 searches each, sized by the shared 200-call session pool, not the per-agent knob) + 3 trimmed retries. Phase 4: wrote **Appendix J** (4 derivations, 19 equations — pass@k estimator, FIM permutation argument, GRPO baseline lemma, sample-and-select scaling), rewrote §12 (8 subsections) and §13 (8 subsections), wired §§7/8/9 to Appendix J, added 13 references and acquired 10 papers. Fixed the `index.md`/`order.json` drift and injected 27 top-level `sec-N` anchors. Dispatched an Opus adversarial re-derivation (R-MATHREV / `[opt:MATH-REDERIVE]`).
- **Result**: All gates green — lint-math 0, validate-refs 0/0, 546 fragment links 0 dangling, 110 references 0 errors (97 strong-local), depth-tiers 19 labels 0 violations, drift-diff 0 TIER-DRIFT, cross-link "no gaps", record-ids consistent. Filed `decisions/2026-08-14-03`, `bugs/2026-08-14-01`, `todos/2026-08-14-llms-for-coding-followups`; closed `todos/2026-08-14-top-level-section-anchors`.
- **Findings**: Four. **(a) A high-severity citation error was found by accident** (`bugs/2026-08-14-01`): §8.3 read a two-column `Valid｜Test` table as a before/after pair, reporting a ~2.9-point gain where the source shows ~11.6, with two of four numbers appearing nowhere in the paper. A substring check would PASS — what is wrong is the row/column assignment. Found by chance means the pre-existing body's ~180 markers are an *unaudited population*, not a clean one. **(b) I deferred a fix on a wrong cost model and had to correct myself**: the missing top-level anchors were filed as a todo assuming the fix cascaded every paragraph anchor; the two healthy surveys use an anchor-only marking with *no* cascade (`renumber-paragraphs --check` → `Updates: 0`). Checking how the working examples actually do it beat reasoning about the fix from the grammar. **(c) The depth-tier drift-diff was silently not binding** — my outline table used `sec-4` where the tool's contract wants the bare token `4`, so its "0 TIER-DRIFT" was vacuous until fixed; a green gate that never looked. **(d) Agent deaths were all step-cap, never context**: 4 of 8 clusters died mid-loop with ~28–31 tool calls and an intermediate-narration final message, and every one was survivable *only* because DRS-HARDEN makes the incrementally-written `_scratch` file the deliverable.

## Conversation 14
- **Request**: "list all items in todos/2026-08-14-llms-for-coding-followups.md", then "finish all of them".
- **Actions**: Closed all six. Two orchestrated workflows: a **citation audit** (16 Sonnet source-grouped verifiers + an Opus adversarial refute stage, 26 agents) and an **evidence extraction** over 19 newly acquired sources (7 Sonnet extractors + 7 Opus completeness critics, 14 agents). Acquired 19 PDFs by direct arXiv fetch (191→210) and rebuilt the SWE-bench SOTA table from the official per-submission `results.json` files (134 submissions). Rewrote §11, §14, §15, §16, §12.7, added §7.5 and §18.6. Filed `bugs/2026-08-14-03` (high) + `-04` (med), a field note, and `todos/2026-08-14-survey-residual-gaps`; closed the parent todo. references 110→130.
- **Result**: All gates green (lint-math 0, validate-refs 0/0, 576 fragment links 0 dangling, 130 refs 0 errors index-mode, 0 TIER-DRIFT, crosslink no gaps). Audit: **196 claims checked, 186 correct, 10 alleged, 7 upheld, 3 refuted.**
- **Findings**: Four. **(a) The adversarial refute stage earned its cost immediately — 3 of 10 alleged citation errors were false alarms** (a ratio verbatim in its source; a claim scoped to the wrong section; a sentence carrying two citations where only one was audited). An audit that applies findings unchecked damages correct prose at a material rate; "the survey is wrong" is itself a claim needing verification. **(b) A second wrong-axis table read** (`bugs/2026-08-14-03`) makes it a *class*, not an incident — both had correct digits and wrong meaning, and both are invisible to a substring check. **(c) Six of seven completeness critics returned `sufficient: false`, and every one was about TONE outrunning evidence, not thin evidence** — they recovered that a headline "+8.2% from code data" is pre-cooldown at 470M only (post-cooldown: +1.5%, with world knowledge going to the text-only arm), that a "logic" benchmark has N=22 so its headline gain is one question, and — computing Fisher exact tests the papers omit — that two of three flagship multi-agent effects do not clear significance. **(d) Two items were filed as blocked on a spent search budget and were closed with `curl`**: the SWE-bench leaderboard publishes structured data, and the arXiv API (HTTPS; the HTTP endpoint returns empty) answered the code-to-reasoning question two agents had died searching for. "Not available" was "not available that way".

## Conversation 15
- **Request**: `/deep-research-survey max mode on Mechanistic Interpretability: A First-Principles Survey`.
- **Actions**: Requested title was again verbatim an existing survey's H1, so Phase 1 measured: body solid (18,119 words, 15 eqs) but two specific holes — **zero 2026 citations** (an arXiv sweep found 60 MI + 50 SAE papers from 2026 alone) and five math appendices at ~670 words each under a title promising first principles. Gated at P0-3; reader chose deepen-plus-refresh, full sweep + weak-ref upgrade, and the **full rendered figure pipeline** (over my ASCII recommendation). Acquired 19 sources via arXiv API + direct fetch (neither touches the WebSearch pool), refs 81→100. Ran two workflows: a 12-agent 2026 frontier sweep and a 10-agent per-appendix derivation extraction, both Sonnet-extract + Opus-critique — 22 agents, 0 errors, 86 evidence records + 5 audited derivation sets. Delivered 3 deterministic figures (the survey had none), Appendix D rewritten around a derivation no source performs, §5.7 (the 2026 reliability audit), and figure captions with numeric disclosure.
- **Result**: All gates green (lint-math 0, validate-refs OK, 262 fragment links 0 dangling, 100 refs 0 errors, 22 depth-tier labels, 0 TIER-DRIFT with the diff genuinely binding, crosslink no gaps). Filed `bugs/2026-08-15-01` and `todos/2026-08-15-mi-survey-remaining-appendices` (A/C/E + body updates + R-MATHREV owed).
- **Findings**: Four. **(a) A workflow critic handed a truncated payload reports false gaps, confidently and with remediation advice.** I inlined `JSON.stringify(res).slice(0,15000)`; four of six critics then alleged papers had returned zero records and advised re-running ~12 agents. All 19 papers had records. The correlation is exact — every cluster alleging a gap exceeded the window, the only one alleging none saw ~100%. A false-green *inversion*: a gate reporting failure that did not occur, with the same surface presentation as the well-known step-cap death. **(b) The Opus math auditors caught a load-bearing transcription error**: a capacity exponent transcribed as `e^{C(d/d′)²δ²}` where the source prints `e^{C(d/d′²)δ²}` — the difference between capacity exponential in **d²** and in **d** — verified against the proof's own closing line. Also that ROME's Eq. 9 is *dimensionally wrong as printed* (missing trace) and its Eq. 4 hooks two different token positions the extraction collapsed into one. **(c) A critic overturned my own Phase-1 framing.** I wrote that weight-space interpretability was "a new axis the survey does not have"; §4.4 already covers it, tagged `[catalog-only]`. I had asserted a negative without checking the file that refutes it — the same error class the citation-integrity rule targets, committed in a brief rather than a citation. Corrected in the outline rather than silently. **(d) A figure derived before its evidence arrived agreed with it.** Appendix C's closed form puts the two-component patching interaction at −13% of the joint effect at moderate size and −77% at large; the 2026 empirical cluster independently reports that the standard estimand conflates a component's isolated effect with exactly that interaction, guaranteed present by the residual stream's skip connections.

## Conversation 16
- **Request**: "finish all items in todos/2026-08-15-mi-survey-remaining-appendices.md".
- **Actions**: Deepened appendices **A, B, C, E** from their audited derivation material (A 754→3,886 words / 6→17 equations; B 940→2,792; C 1,007→2,674; E 610→2,351; survey 29,446→41,437 words, 60 equations). Repaired rather than reproduced the three named source defects, each re-verified by me against the PDF: the capacity exponent `e^{C1(d/d'^2)δ²}`; ROME Eq. 9's missing trace; ROME Eq. 4's two token positions. Acquired Elhage et al. 2021 — the origin of Appendix A's entire framing, previously a weak `(web)` reference — under a new `download/web-native/` convention (`decisions/2026-08-15-01`), taking references 100→102 and weak-form 19%→16.7%. Landed the four body updates (§6.5, §12.2, §4.4→load-bearing, §8.1, new §7.7 "Knowing is not steering", §13.1, two new §15.1 open problems). Ran **five** independent re-derivations on Opus (D, A, B, C, E), deriving before reading, and applied their findings. Filed `bugs/2026-08-15-02` (MEMIT update non-conformable, live on main) and `bugs/2026-08-15-03` (figure SVGs not byte-reproducible), plus `todos/2026-08-15-web-native-source-upgrades.md` and `field-notes/2026-08-15-mi-appendix-deepening.md`.
- **Result**: Todo closed, all three acceptance criteria met. `normalize-survey` → CLEAN; lint-math 0 errors, validate-refs OK, bare-refs 0, 102 references 0 errors, 22 depth-tier labels 0 violations, 0 TIER-DRIFT, 362 fragment links 0 dangling, no cross-link gaps, record-ids consistent.
- **Findings**: Four. **(a) The re-derivation pass returned 12 ERROR / 15 UNSTATED HYPOTHESIS / 13 OVERSTATEMENT while every displayed equation was correct as printed.** That distribution *is* the result — the errors lived in the prose wrapped around correct algebra, exactly the class the rule's "oracles test values; re-derivation tests reasoning" line predicts, and exactly what a value-checking oracle would have passed. **(b) One finding inverted a conclusion I had already written.** §C.7 argued the NIE-sum-vs-NIE-all comparison is uninformative about synergy because the two nulls "bracket" the data; the bracketing holds for 1 of 5 models, and since `1+e ≤ e^e` makes `exp(Σeᵢ)−1` a strict *upper* bound on any independent-multiplicative model, four of five measurements exceed it and thereby *establish* synergy. The section now argues the opposite, and the source's claim comes out better supported than the test it used. **(c) §B.8's thesis was false of the sections it named**: for PD `M = RᵀR` the map `u ↦ Ru` carries M-cosines to Euclidean cosines with zero distortion, so packing and recovery counts are metric-*invariant* (checked to 1e-15). The metric critique is real but belongs on the *measured* quantities, not the existence bounds. **(d) Three of the twelve errors were basis errors** — a gradient ratio quoted in the softmax's input basis but attributed to the query/key parameters (off by exactly `√d_k = 8`), and `m` meaning "bottleneck" and "feature count" one line apart inside the paragraph whose conclusion inverts with the ratio's orientation. All invisible to inspection, all obvious to arithmetic.

<!-- LOG-END -->
